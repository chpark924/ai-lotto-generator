# 금손로또 종합 재점검 리포트 (2026-08-06)

시니어 개발자 · 보안책임자 · 시니어 기획자 · 시니어 UI/UX 디자이너 관점에서, `APP_REVIEW_2026-08-04.md`(전전일 리포트) 이후 쌓인 QA_LOG #29~#48(8/5~8/6, 20개 항목)을 코드 레벨로 직접 재검증했다. `npx tsc --noEmit` / `npx eslint .` / `npx jest --selectProjects unit,components` / `npm audit --omit=dev`를 실제로 실행했고, 그 결과와 코드 직독을 근거로 점수를 다시 매겼다.

## 종합 점수 비교

| 영역 | 8/4 점수 | 8/6 재점검 | 변화 |
|---|---|---|---|
| 보안 | 91 | **92** | ▲ 1 |
| 안정성 | 78 | **87** | ▲ 9 |
| 최적화 | 83 | **86** | ▲ 3 |
| 사용자 편의성(UX/UI) | 85 | **89** | ▲ 4 |
| 경쟁 앱 대비 강점 | 80 | **84** | ▲ 4 |
| **종합** | **~83** | **~88 / 100** | ▲ 5 |

8/4 이후 이틀간 실기기 검증을 동반한 굵직한 개선이 많았다: 여러 세션에 걸쳐 반복 보고되던 "당첨번호 조회 실패"의 진짜 원인(dhlottery.co.kr 프론트엔드 전면 개편)을 최종적으로 찾아 해결(#37), 핫패스 성능 버그를 벤치마크로 검증해 4배 개선(#42), 오늘(8/6) 사용자가 명시적으로 "중요하다"고 지목한 결과 화면 정보 과부하 문제를 배치/게임 단위 배지 분리로 해소(#48). 다만 이 과정에서 **정부 차단 도메인을 실제 코드에 잘못 반영했다가 실기기 검증으로 걸러낸 사고(#36)**가 있었다는 점은 이 프로젝트의 검증 프로세스(로그·재확인 습관)가 실제로 작동했다는 신호이자, 동시에 "그럴듯해 보인다"만으로 외부 신뢰 판단을 내리면 안 된다는 교훈으로 이번 리포트에도 반영했다.

---

## 1. 시니어 개발자 관점 — 안정성 · 코드 품질

### 확인한 강점
- `npx tsc --noEmit`, `npx eslint .` 둘 다 이번 재점검에서도 클린. `npx jest --selectProjects unit` **20개 스위트 168개 테스트 전부 통과**, `components` 프로젝트도 통과(8/4 리포트 시점엔 없었던 `components` 렌더링 테스트가 이제 정착됨).
- **당첨번호 조회 실패의 근본 원인을 마침내 특정·해결**(#37): 여러 세션 동안 헤더·재시도 같은 대증 처방만 반복되던 문제를, 실기기와 연결된 브라우저 도구로 네트워크 탭을 직접 캡처해 `dhlottery.co.kr`이 `common.do`/`gameResult.do` 구 경로를 폐지하고 `lt645/selectPstLt645Info.do`로 개편한 사실을 확정했다. 사용자 로컬 실행으로 1~1238회 전체를 단 한 번 요청으로 성공 확인 — 추측이 아니라 실측으로 마무리된 드문 사례.
- **핫패스 성능 버그를 실측으로 발견·수정**(#42): `overlapCount()`가 호출마다 `new Set()`을 새로 만들던 것을 배열 `includes()`로 교체, 100만 후보×저장번호 20개 워크로드 기준 4.6초→1.15초(약 4배)를 Node 벤치마크로 직접 검증. AI 조합 탐색·운명의 신 등 4개 경로에 동시에 적용됨.
- **진행률 UI가 실제 연산과 항상 일치하도록 재설계**(#34): 100만 회 탐색 시 "100%에서 멈춘 것처럼 보이는" 문제가, 점수 계산 단계에 진행률 보고가 아예 없었던 게 원인임을 찾아내 3단계(생성/점수계산/최종화)로 나눠 항상 단조 증가하도록 수정. 실제 결과가 준비된 시점에만 100%를 보고하도록 해 구조적으로 "멈춤"이 불가능하게 만든 방식이 견고하다.
- 오늘(8/6) 배지/설명 리팩터링도 타입/린트/테스트 전부 통과 확인 — `tests/resultBadges.test.ts`/`tests/explain.test.ts`를 새 API에 맞춰 갱신했고 회귀 없음.

### 새로 확인한 리스크
- **`newArchEnabled: false`(app.json)가 유예 기간이 있는 시한부 설정**이라는 점이 확인됐다. Expo 공식 문서 기준 SDK 54가 레거시 아키텍처를 끌 수 있는 마지막 버전이고, SDK 55부터는 이 설정 자체가 무시된 채 강제로 New Architecture로 동작한다. 지금 당장 문제는 없지만(SDK 54.0.36 사용 중), 다음 SDK 업그레이드 전에 New Architecture 호환성 검증을 미리 계획해두지 않으면 업그레이드 시점에 몰아서 디버깅해야 할 수 있다.
- **QA_LOG #41에서 지적만 하고 아직 착수하지 않은 항목 5개가 8/6 현재도 그대로 남아있다**(코드로 재확인): `SettingsSheet.tsx`의 `Dimensions.get("window")`이 여전히 모듈 로드시 1회성 값(회전/폴더블 대응 안 됨), `NumberGrid.tsx`의 `CELL_SIZE=44`px 고정(태블릿에서 여백 낭비), 로또 공 숫자·배지류에 `maxFontSizeMultiplier` 제한 없음(시스템 큰 글씨 설정 시 잘림 가능), `app.json`의 최상위 `splash` 키가 Expo에서 deprecated(현재는 하위호환으로 동작하나 다음 SDK에서 `expo-splash-screen` 플러그인 방식 전환 필요), `android_backup_before_clean/` 잔여 폴더 정리 미완(무해하지만 하우스키핑 대상).
- **에셋 용량 이슈 미해결**(#42에서 발견): `assets/tab-icon-home.png`가 859×859px·204KB로, 실제 표시 크기(~24~28dp) 대비 여전히 과도하게 크다. 시각 품질에는 영향 없고 앱 번들 용량만 낭비하는 수준.

**점수: 87/100** (8/4 74→78 대비 +9 — 실기기 검증까지 동반한 근본 원인 해결 2건(당첨번호 조회, 진행률 UI)과 실측 벤치마크를 근거로 한 성능 수정이 명확한 개선. 만점을 주지 않는 이유는 #41의 정적 점검 결과가 지적 이후 이틀이 지나도록 착수되지 않은 채 그대로 남아있고, New Architecture 전환이라는 다음 관문이 새로 확인됐기 때문)

---

## 2. 보안책임자 관점

### 재확인한 강점
- 서버 없음 원칙 유지, CSPRNG 기반 생성 로직 그대로 견고. 하드코딩된 API 키·시크릿 재스캔 결과 없음(`app/`, `src/`, `scripts/` 전체 정규식 스캔).
- **생년월일을 SecureStore(iOS Keychain/Android Keystore)로 이전**(#22) — 위협 모델을 "서버 유출"이 아니라 "기기 자체 접근(분실, 루팅/탈옥, 백업 분석)"으로 정확히 정의하고, 그에 맞는 저장소만 선택적으로 격상한 판단이 합리적이다. "모든 데이터 삭제" 시 SecureStore 키도 함께 지우도록 손봐 불완전 삭제를 방지한 점도 꼼꼼하다.
- **알림(로컬 예약) 기능이 프라이버시 원칙과 일관됨**: `expo-notifications`는 서버 푸시가 아니라 기기 내 로컬 예약 알림만 사용해, "서버 없음" 포지셔닝을 알림 기능 추가 이후에도 그대로 지켰다. Expo Go에서 알림 모듈의 부수효과가 앱을 즉시 죽이는 문제를 발견해 동적 import로 우회한 구현도 실무적으로 탄탄하다.
- **"그럴듯한 도메인"을 신뢰 후보에 잘못 올렸다가 실기기 검증으로 걸러낸 사고 처리 과정**(#35→#36): `donghanglottery.com`을 정황(응답 내용이 그럴듯함, 검색 결과)만으로 신규 공식 도메인이라 오판해 QR 검증 정규식과 안내 링크에 실제로 반영했었다. 이후 사용자의 실기기(한국 네트워크) 검증에서 해당 도메인이 **한국 정부의 법적 차단 사이트(HTTP 451)**임이 드러나 즉시 전면 원복했다. 결과적으로 배포 전에 잡혔지만, 검증 없이 도메인을 신뢰 후보로 승격시켰다는 것 자체는 보안 프로세스 관점에서 기록해둘 가치가 있는 근접 사고(near-miss)다 — 앞으로도 유사한 판단(신규 API, 신규 데이터 소스 채택)에는 실사용 환경 검증을 먼저 거치는 절차가 필요하다.
- `npm audit --omit=dev` 재실행 결과 **15건(moderate 13, high 2)로 8/4와 동일** — 전부 `@expo/config-plugins → xcode → uuid` 체인으로, EAS 빌드 툴체인 전용이며 런타임 앱 번들에는 포함되지 않는다는 결론도 재확인.

### 여전히 남은 사항 (미해결, 저위험으로 재평가)
- dhlottery.co.kr/GitHub raw 요청에 인증서 고정 없음 — 검토 후 의도적 미반영으로 결정(#23, 응답 자체를 `isPlausibleWinningDraw`로 검증하므로 실제 피해 범위가 제한적이라는 분석에 동의).
- 나머지 설정값(선호번호, 알림 시각 등)은 여전히 AsyncStorage 평문 — 생년월일만 선택적으로 격상한 것은 합리적 트레이드오프이나, 원칙상 "저장하는 모든 개인화 데이터의 민감도 재평가"를 한 번쯤 명시적으로 문서화해두면 좋다.

**점수: 92/100** (8/4 91 대비 +1 — 근본적인 새 취약점은 없으나, SecureStore 이전과 근접 사고 처리 과정이 보안 성숙도 측면에서 소폭 플러스. 인증서 고정 미비와 평문 설정 데이터가 여전히 남아 만점은 아님)

---

## 3. 시니어 기획자 관점 — 사용자 편의성 · 정합성

### 이번에 확인한 개선
- **오늘(8/6) 사용자가 명시적으로 "중요하다"고 지목한 문제를 정확히 해결**(#48): "설명이 많아지면 가독성이 떨어지고, 반복되면 이탈률이 올라갈 수 있다"는 우려에 대해, "탐색 1회 단위로만 결정되는 사실"(몬테카를로/EV최적화/휠링/끝수스프레드)과 "조합마다 달라지는 사실"(사카이 패턴)을 구조적으로 분리해, 전자는 화면 상단에 딱 1번만, 후자만 카드별로 유지했다. 카드 10장 기준 반복 노출되던 배지 최대 40개·중복 문장 20개가 사라졌다 — 기획 관점에서 "정보를 줄인 게 아니라 같은 정보를 반복하지 않게 한 것"이라 신뢰도 손실 없이 소음만 줄인 올바른 해법이다.
- **온디바이스 알림(구매 마감 하루전/당일/추첨일)으로 습관성 재방문 유도**: 서버 없이도 리마인드 기능을 구현해, "번호를 만들었지만 실제로 사러 안 감/깜빡함"이라는 실사용 이탈 지점을 메웠다. 경쟁 앱 다수가 푸시 기반인 것과 달리 로컬 예약이라 프라이버시 포지셔닝과도 충돌하지 않는다.
- **홈 화면 히어로/퀵메뉴 리디자인**(#30, #31): 텍스트 전용 버튼에서 아이콘+제목 카드로, 어두운 히어로 카드에서 밝은 카드+그라디언트 버튼으로 전환 — 참고 디자인을 픽셀 단위로 분석해 재현한 점이 꼼꼼하다. 좌우 스와이프로 탭 전환(#33)까지 더해져 첫 화면의 조작감이 한층 자연스러워졌다.
- **당첨번호 조회 실패 문제의 최종 해결(#37)이 갖는 기획적 의미**: 이 문제는 여러 세션에 걸쳐 사용자가 반복적으로 겪었던 핵심 신뢰 이슈였다("숫자 앱인데 숫자를 못 불러온다"는 것보다 치명적인 신뢰 손상은 없다). 근본 원인이 해결됨으로써 이 앱의 다른 모든 신뢰 장치(정직한 확률 고지, QR 재검증 등)가 비로소 제 역할을 할 수 있는 토대가 마련됐다고 볼 수 있다.

### 남아있는 정합성/편의성 격차
- **공식 구매 페이지로의 직접 연결 부재**: 경쟁 앱 다수가 결과 확인뿐 아니라 "동행복권 공식 구매 페이지 바로가기"를 제공하는데, 이 앱은 조회 실패 시/QR 확인 후에만 `dhlottery.co.kr/lt645/result`(결과 페이지)로 연결하고, 번호를 만든 직후 "이 번호로 구매하러 가기"류의 도입부는 없다. 서버 없음 원칙과 무관하게 단순 외부 링크 추가만으로 가능한 낮은 비용의 개선이다.
- 온보딩 부재는 사용자 판단으로 유지 결정된 사항(#22)이라 재지적하지 않음.
- QA_LOG #41에서 지적된 해상도/폴더블 대응 항목들이 미착수 상태로 남아 있어(2장 참고), 실사용 편의성에는 아직 반영되지 않았다.

**점수: 89/100** (8/4 85 대비 +4 — 정보 과부하 문제 해결과 알림 기능이 핵심 근거. 구매 연결 부재가 낮은 비용 대비 개선 여지로 남아 만점에는 못 미침)

---

## 4. 시니어 UI/UX 디자이너 관점

### 확인한 강점
- **배지 정보 구조 재설계가 시각적으로도 일관성 있게 처리됨**: 상단 배치 배지와 카드별 사카이 배지가 동일한 초록 톤(`tints.green`)을 공유해, "같은 종류의 정보"라는 인상을 시각적으로도 전달한다. 실제 코드에서 `tints.purple`은 `result.tsx`의 "운명의 신" 배너(`noticeCard`)에서만 쓰이고 있어, 8/4~8/5 사이에 지적됐던 색상 충돌이 완전히 해소된 상태임을 이번에 재확인했다(grep 결과 `GeneratedGameCard.tsx`의 `purple` 언급은 주석뿐, 실제 스타일 값은 전부 `green`).
- **선그래프 리빌 애니메이션 도입**(#39): 막대그래프에서 "타 앱에서 가장 흔한 형태"인 선그래프로 교체하고 900ms 와이프 애니메이션을 적용 — SVG `strokeDashoffset` 방식 대신 더 예측 가능한 너비-클리핑 방식을 택한 판단이 합리적(플랫폼별 동작 편차 리스크 회피).
- **접근성 커버리지가 넓다**: `app/` 15개 파일 중 12개에 `accessibilityLabel`이 있고, 없는 3개(`_layout.tsx` 2개, `privacy.tsx`)는 각각 네비게이션 프레임 자체(React Navigation이 기본 라벨 제공)이거나 순수 정책 텍스트 페이지라 우선순위가 낮다. 오늘 추가된 상단 배치 배지 행도 동일한 `accessible + accessibilityLabel` 그룹화 패턴을 그대로 따라, 새 기능이 늘어도 접근성 설계 원칙이 흔들리지 않고 있다.

### 남아있는 사항
- 2장에서 지적한 디바이스 대응 5개 항목(고정 셀 크기, 폰트 배율 미제한, `Dimensions` 1회성 값 등)은 UI/UX 디자이너 관점에서도 동일하게 유효한 지적이다 — 특히 시스템 큰 글씨 사용자에게 로또 공 숫자나 배지 텍스트가 잘릴 수 있는 부분은 접근성과도 맞닿아 있어 우선순위를 올릴 만하다.
- 다크모드 수동 토글 부재(시스템 설정만 추종)는 8/4와 동일하게 낮은 우선순위로 남아있음.

**점수: 89/100** (8/4의 UX/UI 통합 점수 85 대비 +4 — 오늘 배지 리팩터링과 그래프 애니메이션이 다듬어졌으나, 폰트 배율·태블릿 대응 등 접근성과 맞닿은 디바이스 대응 이슈가 여전히 미착수라 만점과는 거리가 있음. 기획자 관점과 통합해 위 종합표에서는 "사용자 편의성(UX/UI)" 한 줄로 표기)

---

## 5. 경쟁 앱 대비 포지션

동행복권 공식 앱, "로또 번호 생성기(AI추천/공식구매)", "로또 즐기기", "2026로또머신" 등 국내 인기 로또 생성 앱들의 공통 기능(핫/콜드 번호, QR 당첨확인, 출현빈도·구간분포 통계, 공식 구매 연결)을 기준으로 재비교했다.

### 경쟁력 있는 부분
- **QR 당첨확인**은 이제 경쟁 앱들과 동등한 수준이고, 구현 방식(QR 자체 판독 결과를 신뢰하지 않고 항상 공식 데이터로 재검증)은 오히려 더 신중하다.
- **통계 깊이도 준수한 수준으로 확인됨**: 출현빈도(`computeNumberFrequencies`), 장기 미출현(`getLongestAbsentNumbers`), 홀짝/저고/합계 패턴 평균(`computeCombinationPatternStats`), 합계 추세 그래프(`computeSumTrend`) 등이 경쟁 앱들이 흔히 내세우는 "출현빈도·구간별 분포"와 대등하거나 일부(합계 추세 시각화)는 더 세밀하다.
- **"전문 분석" 배지(몬테카를로/EV최적화/휠링/사카이 분석)는 이번에 조사한 경쟁 앱들에서 확인되지 않은 차별점**이다. 다만 이번 재점검에서도 "실제 로직과 항상 일치해야 한다"는 원칙(커버링 설계처럼 보장 안 되는 성질은 의도적으로 배지화 안 함)이 지켜지고 있음을 코드로 재확인했다.
- **온디바이스 알림**도 조사한 경쟁 앱들에서 흔히 보이는 기능은 아니어서, 프라이버시 포지셔닝을 유지하면서 재방문을 늘리는 실질적 차별점이 될 수 있다.

### 남은 격차
- **공식 구매 페이지 직접 연결 부재**(3장에서 이미 지적) — 여러 경쟁 앱이 갖춘 기능인데 이 앱만 없는, 낮은 비용으로 메울 수 있는 격차.
- 판매점 찾기는 서버 없음 원칙과 상충해 의도적 제외(8/4와 동일 평가, 재지적하지 않음).

**점수: 84/100** (8/4 80 대비 +4 — 알림 기능과 배지 차별점을 새로 반영. 구매 연결 부재가 여전히 남아 만점에는 못 미침)

---

## 우선순위별 개선 제안

**빠른 저비용 개선 (Low effort / 체감 효과 높음)**
1. 번호 생성 결과 화면에 "공식 사이트에서 구매하기" 외부 링크 추가(`dhlottery.co.kr`) — 코드 변경 최소, 경쟁 앱과의 마지막 눈에 띄는 격차.
2. `assets/tab-icon-home.png`를 128~256px 수준으로 리사이즈 — 시각 품질 영향 없이 번들 용량만 절감.
3. `android_backup_before_clean/` 폴더 정리(하우스키핑).

**중기 개선 (QA_LOG #41 미착수 항목 — UX/접근성과 직결)**
4. 로또 공 숫자·배지 텍스트에 `maxFontSizeMultiplier` 적용 — 시스템 큰 글씨 사용자의 텍스트 잘림 방지.
5. `SettingsSheet.tsx`의 `Dimensions.get("window")` → `useWindowDimensions()` 전환(회전/폴더블 대응).
6. `NumberGrid.tsx`의 고정 셀 크기를 화면 폭 기반으로 조정(태블릿 여백 낭비 해소).
7. `app.json`의 `splash` 키를 `expo-splash-screen` 플러그인 방식으로 이전(현재는 하위호환으로 동작 중이라 급하지 않으나 다음 SDK 업그레이드 전 필요).

**장기 계획 (지금 당장 급하지 않으나 미리 인지해야 함)**
8. New Architecture 전환 계획 수립 — SDK 55부터는 `newArchEnabled: false`가 무시되므로, 다음 SDK 업그레이드 시점에 몰아서 대응하지 않도록 지금부터 호환성을 틈틈이 확인.
9. Android edge-to-edge(API 36 강제 적용) 관련 실기기 육안 확인 — safe-area insets는 이미 적용돼 있으나 실기기 검증은 아직 없음.

---

## 검증 방법 및 한계

이 리포트는 코드 정적 분석(관련 파일 직접 읽기), `npx tsc --noEmit`(클린), `npx eslint .`(클린), `npx jest --selectProjects unit`(20개 스위트 168개 테스트 통과) 및 `components` 프로젝트 실행(통과), `npm audit --omit=dev`(15건, moderate 13/high 2, 8/4와 동일), 웹 검색을 통한 Expo New Architecture 정책 확인, 국내 경쟁 로또 앱 기능 조사를 근거로 작성했다. 실기기 사용성 테스트(폰트 배율, 폴더블, edge-to-edge), 스토어 등록 전 최종 QA는 이 세션에서 대체하지 못했다. 특히 New Architecture 전환 항목은 지금 당장의 결함이 아니라 "다음 SDK 업그레이드 전에 계획해야 할 일"이라는 점을 명확히 해둔다.
