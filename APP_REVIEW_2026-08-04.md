# 금손로또 종합 재점검 리포트 (2026-08-04)

시니어 개발자 · 보안책임자 · 시니어 기획자 · 시니어 UI/UX 디자이너 관점에서, `APP_REVIEW_2026-08-03.md`(전일 리포트) 이후의 코드베이스를 처음부터 다시 직접 읽고 점검했다. 전일 리포트가 지적한 14개 항목을 코드 레벨에서 하나씩 재확인하고, `npx tsc --noEmit` / `npx jest` / `npm audit`를 실제로 실행해 정적 리뷰를 검증했으며, 그 과정에서 QA_LOG에 기록되지 않은 새로운 이슈도 발견했다.

## 종합 점수 비교

| 영역 | 8/3 점수 | 8/4 재점검 | 변화 |
|---|---|---|---|
| 보안 | 88 | **91** | ▲ 3 |
| 안정성 | 74 | **78** | ▲ 4 |
| 최적화 | 80 | **83** | ▲ 3 |
| 사용자 편의성(UX/UI) | 78 | **85** | ▲ 7 |
| 경쟁 앱 대비 강점 | 72 | **80** | ▲ 8 |
| **종합** | **~79** | **~83 / 100** | ▲ 4 |

하루 사이에 QA_LOG에 명시적으로 기록되지 않은 수정이 매우 많았다(패키지명, 에러 바운더리, 삭제 확인, 재생성 로딩, 개인정보처리방침, 다크모드, 접근성 라벨 등). 전일 리포트의 Critical 3개·High 4개 중 사실상 전부가 이미 해결돼 있었다. 다만 이번 재점검에서 **전일에는 없던 새로운 심각 버그(로또 연구소 탭의 "내 번호 분석"·"이번 주 리포트" 기능이 죽은 코드로 인해 항상 빈 상태로 나오는 문제)**를 발견해 안정성 점수를 크게 올리지 못했다.

---

## 0. 전일 지적 사항 14건 재검증 결과

| # | 8/3 지적 사항 | 심각도 | 8/4 상태 | 근거 |
|---|---|---|---|---|
| 1 | "AI 조합 탐색" 설명이 "5만 개"로 정합성 오류 | Critical | ✅ 해결 | `app/(tabs)/generate.tsx` — "최대 100만 개"로 수정됨 |
| 2 | 내 번호 삭제 확인 절차 없음 | Critical | ✅ 해결 | `tickets.tsx handleDelete` — `Alert.alert` 확인 다이얼로그 추가됨 |
| 3 | 재생성 버튼 로딩 상태 없음 | Critical | ✅ 해결 | `result.tsx` — `isRegenerating` 상태 + `LottoBallLoader` + 버튼 비활성화 추가됨 |
| 4 | `app.json` 패키지명 플레이스홀더 | High | ✅ 해결 | `com.geumsonlotto.app`로 확정됨 |
| 5 | 앱 아이콘 미확정 | High | ❌ 미해결 | `assets/icon-concepts/`에 반려된 시안 3개만 존재, `app.json`에 `icon` 필드 자체가 없음(기본 Expo 아이콘으로 빌드됨) |
| 6 | 개인정보처리방침 페이지 없음 | High | ✅ 해결 | `app/privacy.tsx` 신규 + `legal/privacy-policy.html`(웹 게시용) 동시 작성, 설정 시트에서 링크 연결 확인 |
| 7 | 전역 에러 바운더리 없음 | High | ✅ 해결 | `src/components/ErrorBoundary.tsx` 신규 + `app/_layout.tsx`에서 최상위 래핑 확인 |
| 8 | 접근성 라벨 전무 | Medium | ✅ 대부분 해결 | `app/` 12개 파일에 `accessibilityLabel` 58건, `Dice45`/`LottoBall`/`NumberGrid` 등 핵심 컴포넌트에도 적용됨. 전일 "전무" 평가는 더 이상 사실이 아님 |
| 9 | 저대비 텍스트(#94A3B8) | Medium | ✅ 해결 | `textMuted`를 `#64748B`로 변경, 흰 배경 대비 약 4.77:1로 WCAG AA(4.5:1) 충족 확인(직접 계산 검증) |
| 10 | `writeJson` 실패 시 무대응 | Medium | ✅ 해결 | 실패 시 `throw`로 명확한 한국어 에러 던지도록 수정, 호출부 대부분 try/catch로 Alert 처리 |
| 11 | QR 스캔 당첨확인 기능 없음(경쟁력 격차) | Medium | ✅ 해결 | `app/generate/qr-check.tsx` 신규, `expo-camera` 도입, 11개 테스트 포함 |
| 12 | 다크모드 미대응 | Low | ✅ 해결 | `src/theme/` 전면 도입(라이트/다크 컬러 토큰, 시스템 설정 자동 추종) |
| 13 | 온보딩 없음 | Low | ❌ 미해결 | 관련 파일/화면 없음 |
| 14 | ESLint/Prettier/CI 없음 | Low | ❌ 미해결 | `.eslintrc`, `.github/workflows` 프로젝트 루트에 없음(node_modules 내부 것만 존재) |

**14건 중 10건 해결, 1건 부분 해결(터치 타겟은 아래 3장 참고), 3건 미해결.** 특히 Critical 3건과 High의 절반이 로그에 남기지 않고도 이미 처리돼 있었던 점은 실행력 자체는 준수하다는 신호다. 다만 이런 수정 이력이 QA_LOG에 전혀 기록되지 않은 것은 추적성 관점에서 아쉽다 — 다음 세션 담당자(사람이든 AI든)가 "무엇이 왜 바뀌었는지" 알 방법이 코드 diff뿐이다.

---

## 1. 시니어 개발자 관점 — 안정성 · 코드 품질

### 신규 발견 — 심각한 회귀 버그

**`app/(tabs)/lab.tsx`의 `loadLabData()` 함수 안에 조기 `return`이 있어, 그 아래 코드 전체가 죽은 코드(dead code)다.**

```ts
const loadLabData = useCallback(async () => {
  const recentDraws = await getRecentDrawsSafe(RECENT_DRAW_SAMPLE_SIZE);
  setDraws(recentDraws);
  setFrequencies(computeNumberFrequencies(recentDraws));
  return recentDraws;              // ← 여기서 함수가 끝나버림

  const history = await getGenerationHistory();   // 이하 전부 도달 불가능
  ...
  setMyAnalysis({ ... });          // 실행되지 않음
  ...
  setWeeklyReport({ ... });        // 실행되지 않음
}, []);
```

`return recentDraws;` 이후의 `getGenerationHistory()`, `getTickets()`, `setMyAnalysis(...)`, `setWeeklyReport(...)` 호출이 전부 도달 불가능한 코드다. 그 결과 로또 연구소 탭의 **"이번 주 리포트" 카드는 항상 나타나지 않고, "내 번호 분석" 카드는 사용자가 번호를 아무리 많이 저장해도 항상 "아직 생성한 번호가 없습니다"로 보인다.** README·QA_LOG 양쪽에 3차 기능으로 "구현 완료"라고 기록된 기능이 실제로는 화면에 한 번도 노출되지 않는 셈이다.

원인 추정: QA 17~18번 항목(재시도 버튼이 실패 여부를 판단할 수 있게 반환값을 추가하는 작업) 과정에서 `return recentDraws`를 추가하면서, 원래 함수 끝에 있던 return을 옮기지 않고 새로 앞에 끼워 넣은 것으로 보인다. ESLint의 `no-unreachable` 규칙이나 TypeScript의 관련 검사가 있었다면 즉시 잡혔을 문제인데, 이 프로젝트에는 린터가 없어(14번 항목과 동일한 원인) 그대로 남았다. `npx tsc --noEmit`은 타입 에러가 아니므로 통과하고, Jest 테스트는 이 화면 컴포넌트 자체를 렌더링하는 테스트가 없어(컴포넌트 테스트 0건, 전부 `src/lib` 순수 함수 테스트) 잡히지 않았다.

**수정은 간단하다** — 조기 `return recentDraws;`를 지우고 함수 맨 끝에서 반환하도록 재구성하거나, 두 블록을 분리해 각각 반환하면 된다. 다만 심각도를 "Critical"로 분류하는 이유는: (1) 실제 배포된 기능이 사용자 눈에는 정상 화면(빈 상태)처럼 보여 QA에서도 놓치기 쉽고, (2) "구현 완료"로 문서화된 기능이 조용히 죽어 있다는 것 자체가 이 프로젝트의 회귀 방지 체계(린트·컴포넌트 테스트)에 구멍이 있다는 신호이기 때문이다.

### 그 외 재확인한 강점
- `writeJson` 실패를 더 이상 삼키지 않고 명확한 한국어 에러로 던지며, 대부분의 호출부(`tickets.tsx`, `SettingsSheet.tsx`, `exclusion.tsx` 등)가 이를 try/catch로 받아 사용자에게 Alert로 안내한다 — 전일 지적된 "조용한 저장 실패"는 실질적으로 해소됐다.
- `AppErrorBoundary`가 `ThemeProvider`보다 바깥에 위치해 테마 초기화 자체의 오류까지 잡도록 설계된 점, 클래스 컴포넌트 제약 때문에 `useAppTheme()` 대신 `Appearance.getColorScheme()`을 직접 쓴 점 등 세부 구현이 꼼꼼하다.
- `random.ts`, `drawCache.ts`, `drawApi.ts`는 코드 품질이 특히 높다. 실기기에서만 드러난 `expo-crypto` 1024바이트 상한 문제를 폴백 체인(1024→256→64→16→1)으로 방어하고, 네트워크 재시도·부분 실패·캐시 폴백까지 계층별로 책임이 명확히 분리돼 있다.
- `npx tsc --noEmit` 클린 통과 확인. `npx jest`로 무작위 추출한 8개 스위트(`random`, `generator`, `drawApi`, `destiny`, `qrCheckFlow`, `parseLottoQr`, `drawStats`, `tickets`)를 직접 실행해 전부 통과를 재확인했다(전체 14개 스위트를 샌드박스 시간제한 내에 다 돌리지는 못했으나, 실패 신호는 전혀 없었다).

### 여전히 남은 개발 리스크
- **ESLint/Prettier/CI 부재**(전일 14번, 미해결) — 이번에 발견한 dead code 버그가 정확히 이 공백 때문에 새어 나갔다. `no-unreachable`, `no-unused-vars`만 켜도 즉시 잡혔을 문제다. 우선순위를 올려야 한다.
- **컴포넌트/화면 레벨 테스트 0건** — 12개 테스트 파일 전부가 `src/lib`의 순수 함수만 검증한다. `lab.tsx` 버그처럼 "함수는 있는데 호출 흐름이 끊긴" 문제는 React Testing Library 같은 렌더링 테스트가 있어야 잡힌다.
- 터치 타겟: `tickets.tsx`의 "공유"/"삭제" 버튼은 `paddingVertical: 4`만 있고 `hitSlop`이나 최소 높이 지정이 없어 44pt 가이드라인에 못 미칠 가능성이 여전히 크다(전일 지적, 미해결).

**점수: 78/100** (전일 74 대비 +4 — Critical 3건이 실제로 고쳐진 점은 명확한 개선이나, 동급으로 심각한 새 버그를 발견해 큰 폭으로 올리지 않았다)

---

## 2. 보안책임자 관점

### 재확인한 강점 (전일과 동일하게 유지)
- 서버 없음 원칙 그대로 유지, `expo-crypto` CSPRNG + rejection sampling 기반 번호 생성 여전히 견고.
- `isPlausibleWinningDraw` 검증 로직 그대로 유지 — 미추첨 회차 오인 방지.
- 하드코딩된 API 키·시크릿 없음(재확인).

### 이번에 개선을 확인한 부분
- **개인정보처리방침 완비**: `app/privacy.tsx`(인앱)와 `legal/privacy-policy.html`(웹 게시용)이 동일한 내용·동일한 시행일자(2026-08-04)로 작성돼 있고, 설정 시트에서 링크로 연결된다. 스토어 심사 요건 충족.
- **패키지명 확정**: `com.geumsonlotto.app`로 iOS/Android 동일하게 정리됨.
- **의존성 취약점 감소**: `npm audit --omit=dev` 재실행 결과 **15건(moderate 13, high 2)**으로, 전일 SECURITY_NOTES.md 기록(30건, moderate 11/high 19)보다 줄었다. 다만 여전히 `@expo/config-plugins → xcode` 체인이 근본 원인이며, 이는 EAS 빌드 툴체인 전용이라 런타임 앱 번들에는 포함되지 않는다(전일 평가와 동일한 결론).
- **QR 카메라 권한 문구**: `app.json`에 "촬영된 이미지는 기기에 저장되거나 외부로 전송되지 않습니다"라고 명시 — 카메라라는 민감 권한을 추가하면서도 최소 수집 원칙을 문서화한 점이 좋다.

### 여전히 남은 보완 사항 (전일과 동일, 미해결)
- AsyncStorage 평문 저장 — 생년월일 등 선택 저장 데이터가 암호화 없이 남는다. 위협 모델상 저위험이지만 여전히 개선 여지.
- dhlottery.co.kr 요청에 인증서 고정 없음 — 응답 자체를 `isPlausibleWinningDraw`로 검증하므로 실질 피해는 제한적.
- 앱 아이콘 미확정 상태로는 스토어 제출 자체가 불가능(보안 이슈는 아니지만 출시 게이팅 요소).

**점수: 91/100** (전일 88 대비 +3 — 프라이버시 정책·패키지명 정리, 의존성 취약점 감소를 반영. 완전한 만점을 주지 않는 이유는 평문 저장·인증서 고정 미비가 여전히 남아 있어서다)

---

## 3. 시니어 기획자 관점 — 사용자 편의성 · 정합성

### 이번에 확인한 개선
- **삭제·재생성 등 되돌릴 수 없는 액션의 확인/피드백 절차가 일관됐다.** 내 번호 삭제(확인 다이얼로그), 재생성(로딩 카드+버튼 비활성화), 전체 데이터 삭제(기존부터 확인 있음) 세 곳 모두 이제 같은 수준의 안전장치를 갖췄다 — 전일 "화면마다 다르다"는 지적이 해소됐다.
- **QR 당첨확인 도입**: 실물 용지를 스캔해 즉시 당첨 여부를 확인하는, 경쟁 앱 대비 가장 아쉬웠던 편의 기능 격차가 메워졌다. 게다가 QR 리버스 엔지니어링 결과(당첨 여부 표시)를 곧바로 신뢰하지 않고 항상 동행복권 공식 데이터로 재검증하는 설계는, 이 앱이 지금까지 지켜온 "신뢰할 수 없는 소스는 검증 후 사용" 원칙과 정확히 일치한다.
- **"AI로 번호 만들기" CTA에 온디바이스 엔진임을 명시**: 홈 화면 히어로 버튼의 `accessibilityLabel`이 "AI로 번호 만들기, 기기 안에서 계산되는 온디바이스 규칙 엔진"으로 바뀌어, 전일 지적한 "AI 네이밍 신뢰성 리스크"에 대한 최소한의 안전장치가 생겼다. 다만 이는 스크린리더 사용자에게만 들리는 문구이고, 화면에 시각적으로 보이는 텍스트는 여전히 "AI로 번호 만들기"뿐이다 — 시각적으로도 짧은 보조 설명을 노출하면 더 좋다.

### 이번에 새로 발견한 정합성 문제
- **로또 연구소의 "내 번호 분석"·"이번 주 리포트" 기능이 항상 빈 상태로 보인다** (1장의 코드 버그와 동일 건, 기획 관점에서 재서술: 기획서 3차 범위로 "구현 완료"라 문서화된 기능이 실제 사용자에게는 한 번도 도달하지 않는다). 사용자 입장에서는 "번호를 여러 개 저장했는데 왜 분석이 안 뜨지?"라는 혼란으로 이어질 수 있고, 이는 QR 스캔처럼 눈에 띄는 신규 기능보다 오히려 신뢰에 더 해로울 수 있다 — "이 앱은 내 데이터를 제대로 안 보고 있나?"라는 인상을 줄 수 있기 때문이다.
- README.md는 여전히 "AI 설명 BYOK 옵션(사용자 API 키 입력 시 AI 호출)"을 지원 기능으로 서술하지만, 실제 코드(`src/lib/ai/explain.ts`)는 이 기능을 완전히 제거하고 로컬 템플릿 설명만 제공한다(SECURITY_NOTES.md 8/31 메모에서 이미 지적됐던 문서-코드 불일치가 8/4에도 그대로 남아 있음). 사소해 보이지만, "문서가 실제 동작과 다르다"는 패턴이 이번에 발견한 dead-code 버그와 같은 계열의 리스크(추적성 부족)라는 점에서 함께 정리할 필요가 있다.

### 여전히 남은 사항 (전일과 동일, 미해결)
- 온보딩 부재 — 5가지 생성 모드 중 무엇을 써야 할지 첫 사용자가 판단할 기준이 없다.

**점수: 85/100** (전일 78 대비 +7 — 확인 절차 일관성, QR 도입, AI 네이밍 리스크 완화를 반영. dead-code 버그와 README 불일치가 감점 요인)

---

## 4. 시니어 UI/UX 디자이너 관점

### 이번에 확인한 개선
- **접근성 라벨이 전면적으로 적용됐다.** 전일 리포트의 "app/ 전체에 accessibilityLabel이 단 하나도 없다"는 평가는 더 이상 사실이 아니다 — `app/` 12개 파일 58건, 핵심 컴포넌트(`Dice45`, `LottoBall`, `NumberGrid`, `SettingsSheet` 등)에도 적용돼 있다. 특히 `Dice45`는 굴리는 중/확정 상태를 실시간으로 `accessibilityLabel`에 반영해 스크린 리더 사용자도 애니메이션 결과를 알 수 있게 했고, `NumberGrid`는 `accessibilityState={{ selected, disabled }}`까지 정확히 채워 넣었다 — 단순히 라벨만 붙인 게 아니라 상태까지 제대로 전달한다는 점에서 완성도가 높다.
- **저대비 텍스트 문제 해결 확인**: `textMuted`를 `#94A3B8`(대비비 약 2.8:1)에서 `#64748B`(직접 계산 결과 흰 배경 대비 약 4.77:1)로 변경 — WCAG AA 기준(4.5:1)을 실제로 충족한다.
- **다크모드 완비**: `src/theme/colors.ts`에 라이트/다크 색상 토큰 세트, 상태 배지(tint) 세트를 모두 별도로 정의하고 `useColorScheme()`으로 시스템 설정을 자동 추종한다. 브랜드 히어로 카드(#0F172A)처럼 의도적으로 테마 불변이어야 하는 요소와, 배경/텍스트처럼 테마를 따라야 하는 요소를 명확히 구분해 설계한 점이 특히 좋다 — 다크모드를 "일단 색만 반전"하는 수준이 아니라 토큰 설계 단계부터 구조적으로 접근했다.

### 여전히 남은 사항
- 터치 타겟(공유/삭제 텍스트 버튼) 여전히 작음 — 전일과 동일, 미해결.
- 온보딩 부재 — 3장과 동일.
- 다크모드에 수동 토글이 없고 시스템 설정만 따른다(주석에 "요청 범위 밖"이라 명시돼 있어 의도된 설계로 보이나, 시스템 다크모드를 켜지 않는 사용자는 앱 내에서 직접 전환할 방법이 없다 — 우선순위 낮은 개선 여지).

**점수: 85/100** (전일 76 대비 +9 — 접근성·대비·다크모드 세 가지 굵직한 지적이 모두 실제로 해결된 것을 코드로 직접 확인했다. 기획자 관점 점수와 통합해 위 종합표에서는 "사용자 편의성(UX/UI)" 한 줄로 표기했다)

---

## 5. 경쟁 앱 대비 포지션

### 전일 대비 달라진 점
- **QR 스캔 당첨확인 도입으로 가장 큰 격차가 해소됐다.** 전일 리포트에서 "실사용 편의성에서 눈에 띄는 격차"로 지적한 항목이 하루 만에 메워졌고, 구현 방식도 신중하다 — QR 자체 리버스 엔지니어링 결과를 신뢰하지 않고 항상 공식 데이터로 재검증하는 구조는 오히려 "QR 스캔은 되는데 가끔 틀리다"는 평판 리스크가 있는 경쟁 앱 대비 더 안전하다.
- 프라이버시·정직한 확률 고지·원터치 전체 삭제라는 기존 차별점은 그대로 유지되고, 여기에 개인정보처리방침 페이지까지 갖춰 "프라이버시 우선"이라는 포지셔닝을 스토어 등록 단계에서도 뒷받침할 수 있게 됐다.

### 여전히 남은 격차
- 명당 판매점 찾기 — 서버 없음 원칙과 상충하므로 의도적 제외로 판단됨(전일과 동일 평가).
- 통계 분석 깊이 — 로또데이터랩류 대비 여전히 얕다. 더구나 이번에 발견한 "내 번호 분석"·"이번 주 리포트" dead code 버그 때문에, 원래 계획된 개인화 분석 기능조차 지금은 사용자에게 도달하지 못하고 있어 체감 격차가 문서상 격차보다 더 크다 — 이 버그를 고치는 것 자체가 경쟁력 회복의 일부다.

**점수: 80/100** (전일 72 대비 +8 — QR 도입이 핵심 근거. dead-code 버그로 인해 개인화 분석 기능이 실질적으로 없는 것과 마찬가지인 상태라는 점을 감안해 만점 근접까지는 주지 않았다)

---

## 우선순위별 개선 제안 (갱신판)

**바로 고쳐야 함 (Critical)**
1. `app/(tabs)/lab.tsx`의 `loadLabData()` 조기 `return` 제거 — "내 번호 분석"·"이번 주 리포트" 기능 복구. 수정 자체는 5분 내로 가능하나, 방치 시 "구현됐다고 문서화된 기능이 실제로는 없는" 상태가 계속된다.
2. 프로�젝트 루트에 ESLint(`no-unreachable`, `no-unused-vars` 최소한이라도) 도입 — 위 버그가 이런 최소 설정만으로도 즉시 잡혔을 문제였다.

**출시 전 정리 (High)**
3. 앱 아이콘 확정 (전일부터 이월, 3개 시안 전부 반려된 채 미해결 — 새 컨셉 필요).
4. README.md의 BYOK(AI 자연어 설명) 서술을 실제 동작(로컬 템플릿 전용)에 맞게 정정.

**중기 개선 (Medium)**
5. 화면/컴포넌트 렌더링 테스트 최소 1~2건이라도 도입(React Testing Library) — 순수 함수 테스트만으로는 이번 같은 "호출 흐름이 끊긴" 버그를 못 잡는다.
6. `tickets.tsx`의 공유/삭제 버튼 터치 타겟을 44pt 이상으로 확대(`hitSlop` 또는 `minHeight`).
7. AsyncStorage 저장 데이터(특히 생년월일) 암호화 검토.

**여유 있을 때 (Low)**
8. 온보딩/모드 추천 플로우.
9. 다크모드 수동 토글(시스템 설정과 별개로 앱 내에서 전환).
10. dhlottery.co.kr 요청에 대한 인증서 고정 검토.
11. 최소 CI(테스트 자동 실행 + lint) 구성.

---

## 검증 방법 및 한계

이 리포트는 코드 정적 분석(전체 파일 직접 읽기), `npx tsc --noEmit`(클린 통과 확인), `npx jest`(8/14 스위트 실행 확인, 전부 통과, 시간제한으로 나머지는 미실행), `npm audit --omit=dev`(15건, moderate 13/high 2 확인), WCAG 대비비 수동 계산(`#64748B` on `#FFFFFF` ≈ 4.77:1)을 근거로 작성했다. 실기기 사용성 테스트, 스크린리더 실제 구동, dhlottery.co.kr 실서버 연동(샌드박스에 실인터넷 없음)은 대체하지 못하므로 실기기 재확인을 권장한다. 특히 오늘 발견한 `lab.tsx` dead code 버그는 실기기/시뮬레이터에서 "내 번호 분석" 카드가 계속 빈 상태로 보이는지 확인하는 것으로 손쉽게 재현 가능하다.
