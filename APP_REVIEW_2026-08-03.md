# 금손로또 종합 점검 리포트 (2026-08-03)

시니어 개발자 · 보안책임자 · 시니어 기획자 · 시니어 UI/UX 디자이너 관점에서 전체 코드베이스(약 60개 파일, app/ · src/ · tests/)를 직접 읽고 점검한 결과다. 코드 실행 흐름, 저장 방식, 네트워크 호출, 화면 UX, 유사 앱 대비 포지션을 기준으로 평가했다.

## 종합 점수

| 영역 | 점수 | 한줄평 |
|---|---|---|
| 보안 | 88 / 100 | 서버가 없어 구조적으로 안전. 사소한 보완 여지만 있음 |
| 안정성 | 74 / 100 | 최근 QA로 많이 좋아졌지만 미확정 실패 처리·확인 없는 삭제 등 실사용 리스크 존재 |
| 최적화 | 80 / 100 | 난수 풀링 등 핵심 병목은 이미 해결. 사소한 재렌더링 낭비만 남음 |
| 사용자 편의성(UX) | 78 / 100 | 흐름 설계는 좋으나 접근성·확인 절차·문구 정합성에 구멍 |
| 경쟁 앱 대비 강점 | 72 / 100 | 프라이버시·정직한 확률 안내는 차별점, 편의 기능은 후발 |
| **종합** | **~79 / 100** | 출시 전 다듬으면 준수한 완성도. 지금은 "베타"에 가까움 |

---

## 1. 시니어 개발자 관점 — 안정성 · 코드 품질

### 발견한 실제 버그

**① 결과 화면 "같은 조건으로 다시 생성" — 로딩 상태 전무**
`app/generate/result.tsx`의 `handleRegenerate()`는 AI_SEARCH 모드일 때 `generateAiSearchGames`를 그대로 호출하는데, 어떤 로딩 표시도 버튼 비활성화도 없다. 오늘 탐색 강도를 최대 100만 회로 올렸기 때문에, 실기기에서 이 버튼을 누르면 수 초~수십 초간 화면이 아무 반응 없이 멈춘 것처럼 보인다. 연타하면 여러 번 중복 실행될 수도 있다. AI 조합 탐색 화면 자체에는 이미 훌륭한 진행률 UI(`LottoBallLoader`)가 있는데, 정작 재생성 경로에는 빠져 있다.

**② 내 번호 삭제 — 확인 절차 없음**
`app/(tabs)/tickets.tsx`의 `handleDelete`는 `Alert.alert` 확인 없이 바로 `deleteTicket`을 호출한다. 반면 설정 화면의 "모든 로컬 데이터 삭제"는 확인 다이얼로그를 거친다. 같은 앱 안에서 삭제 동작의 안전장치 수준이 다르다 — 리스트에서 실수로 삭제 버튼을 터치하면 되돌릴 방법이 없다.

**③ 쓰기 실패가 조용히 사라짐**
`src/lib/storage/storage.ts`의 `readJson`은 실패 시 fallback을 반환하도록 방어되어 있지만, `writeJson`은 별도 try/catch가 없다. `saveTicket`, `updateTicketStatus` 등 대부분의 쓰기 경로도 호출부에서 에러를 잡지 않는다. AsyncStorage 쓰기가 실패하면(용량 초과, 직렬화 오류 등) 사용자는 "저장했습니다" 흐름을 그대로 보고 안심하지만 실제로는 저장되지 않았을 수 있다. 발생 확률은 낮지만, 발생하면 사용자가 알아챌 방법이 없다.

**④ 번호 만들기 허브의 설명 문구가 오늘자 변경과 어긋남**
`app/(tabs)/generate.tsx`의 MENU_ITEMS에 "AI 조합 탐색"이 여전히 "기기 안에서 최대 5만 개의 후보를 비교"라고 적혀 있다. 오늘 세션에서 탐색 강도를 100만 회까지 올렸으므로 이 문구는 이미 사실과 다르다. 같은 세션 안에서 발생한 정합성 누락이라 바로 잡아야 한다.

**⑤ 전역 에러 바운더리 없음**
`app/_layout.tsx`에 React Error Boundary가 없다. 화면 렌더링 중 예외가 하나라도 나면 RN 기본 빨간 에러 화면(또는 흰 화면)으로 떨어지고 사용자가 복구할 방법이 없다. 지금까지 QA에서 잡은 이슈들은 대부분 "조용한 실패"였는데, 그 반대인 "요란한 크래시"에 대한 마지막 안전망이 없는 셈이다.

### 잘 되어 있는 부분
검증 로직(`validateGenerationRequest`)이 경계값을 꼼꼼히 체크하고, 사용자 친화적 한국어 에러 메시지를 던진다. `ValidationError`처럼 목적이 분명한 커스텀 에러 클래스를 쓴다. 테스트가 12개 파일에 걸쳐 폭넓게 있고(`generator`, `validators`, `scoring`, `similarity`, `pattern`, `probability`, `rank`, `destiny`, `luckyNumber`, `random`, `drawApi`, `drawStats`, `tickets`), 특히 `random.test.ts`는 "실기기에서만 터지던 버그"를 회귀 방지 테스트로 만들어둔 게 인상적이다. 다만 ESLint/Prettier 설정과 CI(GitHub Actions 등)가 전혀 없어서, 지금까지의 코드 품질은 전적으로 리뷰 세션의 꼼꼼함에 의존하고 있다 — 자동화된 안전망이 없다.

**점수: 74/100** (핵심 로직은 탄탄하지만, 위 5가지는 실제 사용자가 부딪힐 수 있는 문제라 감점)

---

## 2. 보안책임자 관점

### 강점
- 서버가 아예 없다. 모든 계산이 기기 내부(로컬 JS)에서 끝나고, 외부로 나가는 유일한 네트워크 호출은 동행복권 공식 조회 엔드포인트(GET, 인증 불필요, 응답만 받음)뿐이다. 수집·전송되는 개인정보가 구조적으로 없다.
- 로또 번호 자체는 `expo-crypto`의 CSPRNG를 쓰고(`Math.random()` 미사용), rejection sampling으로 편향 없이 뽑는다 — 복권 생성기에서 가장 중요한 부분을 제대로 했다.
- 서버 응답을 맹신하지 않는다. `isPlausibleWinningDraw` 검증(회차 일치, 번호 범위/중복, 보너스 번호 유효성)으로 "미추첨 회차를 당첨번호로 오인" 같은 신뢰성 사고를 구조적으로 막아뒀다(오늘 이전 세션에서 발견·수정된 항목).
- 생년월일처럼 민감할 수 있는 정보는 기본값이 "저장 안 함"이고, 저장 여부를 사용자가 매번 선택한다(`saveBirthProfile`). AI 설명 생성에도 원본 개인정보 대신 파생된 구조화 특징만 쓴다(`GameFeatures`) — 기획서 수준에서 개인정보 최소화 원칙을 지키려 한 흔적이 코드에 실제로 있다.
- 설정 화면의 "모든 로컬 데이터 삭제"가 진짜로 즉시 전체 삭제된다(`clearAllLocalData`) — 원터치 완전 삭제권을 사용자에게 실질적으로 보장하는 앱은 드물다.
- 코드 전체를 grep해도 하드코딩된 API 키·시크릿·토큰이 없다(서버가 없으니 애초에 필요도 없음).

### 보완이 필요한 부분
- AsyncStorage는 평문 저장이다. 위협 모델상 "탈옥/루팅된 기기에서 로컬 파일 접근"이 아니면 큰 문제는 아니지만, 생년월일을 저장하기로 선택한 사용자 데이터는 암호화 없이 평문으로 남는다.
- 외부 네트워크 요청(dhlottery.co.kr)에 인증서 고정(certificate pinning)이 없다 — 공용 와이파이에서 MITM으로 응답이 조작될 이론적 여지가 있다. 다만 응답 내용 자체를 `isPlausibleWinningDraw`로 검증하므로 실질 피해 범위는 제한적이다.
- `app.json`의 iOS/Android 패키지명이 아직 `com.yourcompany.ailotto` 플레이스홀더 그대로다. 보안 이슈는 아니지만 스토어 심사·브랜드 신뢰도 관점에서 반드시 출시 전 정리해야 한다(이미 QA_LOG 11번에 논의됐고, 의도적으로 보류 중인 상태).
- 개인정보처리방침(Privacy Policy) URL이 앱 어디에도 없다. 실제로 서버 수집이 없다는 점은 강점이지만, 스토어 등록 단계에서는 "수집을 안 한다"는 사실 자체를 명시한 정책 페이지가 있어야 심사를 통과하기 쉽다.

**점수: 88/100**

---

## 3. 시니어 기획자 관점 — 사용자 편의성 · 경쟁력

### 사용자 편의성에서 잘한 결정들
- "회차 번호"를 사용자에게 직접 노출하지 않고 "이번 주(8/8)" 식으로 날짜로 바꿔 보여주는 결정(오늘 이전 세션), 저장/구매예정/구매완료/확인완료 상태를 색으로 구분한 것, 주요 액션과 보조 액션의 시각적 위계를 분리한 것(45면체 주사위, 내 번호 탭) 모두 실사용자 피드백을 반영해 다듬어진 흔적이 뚜렷하다.
- 확률에 대한 메시지가 시종일관 정직하다 — "탐색 횟수가 늘어도 개별 확률은 동일하다"를 여러 화면에서 반복 고지한다. 이건 사용자를 오도하지 않으려는 의도적 제품 원칙이고, 장기적으로 신뢰를 쌓는 방향이다.
- 로컬 알림(구매 마감 하루 전 / 당일 / 추첨 임박) 3단계 설계는 실제 사용 시나리오(깜빡하고 구매를 안 함)를 잘 짚었다.

### 보완이 필요한 부분
- **"AI 조합 탐색" 네이밍의 신뢰성 리스크**: 코드 주석에 명시되어 있듯 실제로는 로컬 규칙 기반 엔진이고 LLM 호출이 전혀 없다. 화면 안에는 "온디바이스 연산" 문구로 정직하게 밝히고 있지만, 첫 진입 시(허브 화면, 홈 화면 CTA "AI로 번호 만들기")에는 그런 설명이 없다. 앱스토어 리뷰나 사용자 인지 관점에서 "AI라며 그냥 랜덤이잖아" 같은 반응이 나올 여지가 있다. 이름을 바꾸거나, 최초 진입 시점에 "온디바이스 규칙 엔진" 설명을 한 번은 노출하는 게 안전하다.
- **삭제·재생성처럼 되돌릴 수 없거나 시간이 걸리는 액션에 확인/피드백 절차가 화면마다 다르다** (위 개발자 섹션 ①②와 동일 이슈, 기획 관점에서도 일관성 문제로 다시 짚을 만하다).
- **온보딩이 없다.** 5가지 생성 모드(제외/AI탐색/행운번호/주사위/운명의신)가 허브에 나열만 되어 있고, 처음 쓰는 사용자가 "나한테 맞는 모드가 뭔지" 판단할 기준이 없다. 짧은 모드 추천 질문(예: "번호 취향이 있나요/그냥 맡길래요")이나 대표 모드 하이라이트가 있으면 이탈을 줄일 수 있다.

**점수: 78/100**

---

## 4. 시니어 UI/UX 디자이너 관점

### 강점
- 이번 세션에서 진행한 버튼 위계(Primary/Secondary/Tertiary) 정리, 스켈레톤 도입, 저장 피드백(토스트+하이라이트)은 정확히 UX 성숙도를 높이는 방향의 개선이었다.
- 45면체 주사위 애니메이션(`Dice45`), 로딩 스피너 대체 애니메이션(`LottoBallLoader`) 모두 외부 라이브러리 없이 RN Animated만으로 손으로 짠 디테일이 상당히 공들여 있다 — 카피캣 느낌이 아니라 브랜드 개성이 있다.
- 색상 시스템(브랜드 블루 #2563EB, 로또공 5색 구간)이 화면 전반에 일관되게 쓰인다.

### 보완이 필요한 부분
- **접근성(Accessibility)이 사실상 전무하다.** `app/` 전체를 검색해도 `accessibilityLabel`/`accessibilityRole`이 단 하나도 없다. 스크린 리더(TalkBack/VoiceOver) 사용자는 로또공이 어떤 숫자인지, 버튼이 무슨 동작인지 전혀 알 수 없다.
- **보조 텍스트 색상의 대비가 WCAG AA 기준(4.5:1)에 못 미친다.** `#94A3B8`(슬레이트 계열) 텍스트가 흰 배경 위에 매우 광범위하게 쓰이는데, 실제 대비비는 약 2.8:1 수준이라 저시력 사용자에게는 거의 안 보일 수 있다. 캡션·안내문 전반에 걸친 반복 패턴이라 한 곳만 고쳐서 될 문제가 아니다.
- **다크모드 대응이 없다.** `app.json`의 `userInterfaceStyle: "automatic"`은 시스템이 다크모드일 때 상태바/시스템 UI를 다크로 전환하겠다는 뜻인데, 앱 콘텐츠 자체는 색상이 전부 하드코딩된 라이트 테마다. 시스템은 다크인데 앱만 밝은 채로 남아 이질감을 준다.
- **터치 타겟 크기**는 대체로 준수(버튼 패딩 12px 이상)하지만, 내 번호 탭의 "삭제"/"공유" 텍스트 링크류는 44pt 최소 타겟 가이드라인에 못 미칠 수 있다.

**점수: 76/100**

---

## 5. 경쟁 앱 대비 포지션

같은 카테고리(로또 번호 생성/분석) 국내 앱들(로또퍼스트, 로또데이터랩, 2026로또머신, 로또랩 등)과 비교했다.

### 금손로또가 확실히 앞서는 지점
- **프라이버시**: 대부분의 경쟁 앱은 서버 기반 통계·광고 SDK를 쓰는 반면, 금손로또는 구조적으로 서버가 없어 수집할 데이터 자체가 없다. 코드에 광고 SDK 흔적도 없다(현재 무광고).
- **확률 메시지의 정직함**: "핫넘버", "AI 추천" 류의 경쟁 앱 마케팅은 통계적 근거를 실제 당첨 확률과 혼동하게 만들 소지가 있는데, 금손로또는 모든 화면에서 "탐색은 확률에 영향 없음"을 반복 고지한다. 이는 장기적 신뢰 측면에서 명확한 차별점이다.
- **데이터 주권**: "모든 로컬 데이터 삭제" 원터치 기능은 경쟁 앱 대부분에 없는, 사용자에게 실질적 통제권을 주는 기능이다.

### 경쟁 앱 대비 뒤처지는 지점
- **QR 스캔 당첨확인 없음**: 다수 경쟁 앱(로또퍼스트, 동행복권 관련 앱 등)이 실물 로또용지 QR코드를 카메라로 스캔해 당첨 여부를 바로 확인해준다. 금손로또는 회차를 수동 지정해야 한다 — 실사용 편의성에서 눈에 띄는 격차다.
- **명당 판매점 찾기 없음**: 위치 기반 "잘 되는 판매점" 찾기는 로또퍼스트 등에서 인기 기능인데, 서버/위치 API가 필요해 "서버 없음" 원칙과 상충한다. 의도적으로 뺀 기능이라면 괜찮지만, 사용자 입장에서는 "이 앱엔 왜 없지" 하는 지점이 될 수 있다.
- **통계 분석의 깊이**: 로또데이터랩류는 여러 AI 모델 비교, 로또랩은 특이 패턴 분석 실험실처럼 "분석 자체가 재미"인 콘텐츠가 풍부하다. 금손로또의 "로또 연구소"는 출현 빈도/패턴 통계 정도로, 상대적으로 얕다.

**점수: 72/100** — 방향성(프라이버시·정직함)은 차별화됐지만, 표준으로 자리잡은 편의 기능(QR 확인) 하나는 격차가 크다.

---

## 우선순위별 개선 제안

**바로 고쳐야 함 (Critical)**
1. `generate.tsx` 허브의 "AI 조합 탐색" 설명 문구 정정(5만 → 100만, 오늘자 변경과 불일치)
2. 내 번호 삭제에 확인 다이얼로그 추가
3. 결과 화면 "같은 조건으로 다시 생성"에 로딩 상태/중복 실행 방지 추가

**출시 전 정리 (High)**
4. `app.json` 패키지명(`com.yourcompany.ailotto`) 확정
5. 앱 아이콘 확정(QA_LOG 12번에서 3개 시안 전부 반려된 채로 미해결)
6. 개인정보처리방침 페이지/링크 준비(스토어 등록 요건)
7. 전역 에러 바운더리 추가

**중기 개선 (Medium)**
8. 접근성 라벨(accessibilityLabel/Role) 전 화면 적용
9. 저대비 텍스트 색상(#94A3B8류) WCAG AA 기준으로 조정
10. writeJson 실패 처리 및 사용자 알림 경로 추가
11. QR 스캔 당첨확인 기능 검토(경쟁력 격차 해소)

**여유 있을 때 (Low)**
12. 다크모드 대응
13. 온보딩/모드 추천 플로우
14. ESLint/Prettier + 최소 CI(테스트 자동 실행) 도입

---

*이 리포트는 코드 정적 분석과 화면 흐름 추적을 기반으로 작성했으며, 실기기 사용성 테스트를 대체하지 않는다. 특히 항목 3(접근성)·9(대비)는 실제 스크린리더/저시력 기기에서 재확인을 권장한다.*
