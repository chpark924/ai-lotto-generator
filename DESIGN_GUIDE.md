# 금손로또 디자인 가이드 (v1, 2026-09-10)

> ## Design Principle (고정 — 이 문서·이 프로젝트의 최상위 원칙, 2026-09-10 확정)
>
> **Quiet UI. Crafted Objects.**
> UI는 조용하게, 핵심 오브젝트는 집요하게.
>
> 이 한 줄이 이 문서의 다른 모든 항목(1~14절)보다 우선한다 — 개별 규칙(radius 값, 색 토큰 등)과 이 원칙이 충돌하는 것처럼 보이면 이 원칙이 이긴다. 화면 하나를 수정할 때마다 15절의 6문항 체크리스트를 반드시 통과해야 한다.

## 0. 이 문서의 목적과 만든 방식

사용자가 GPT로 정리한 디자인 가이드 초안("Refined Utility × Crafted Delight")을 받아, 이 앱의 **실제 코드(src/theme, app/(tabs)/index.tsx, generate.tsx, _layout.tsx 등)를 직접 열어 지금 상태와 하나하나 대조**한 뒤 선별·조정한 결과다. GPT 초안을 그대로 옮기지 않고, 다음 세 가지 중 하나로 분류했다.

- **그대로 채택**: 이미 지금 앱과 잘 맞거나, 지금 상태의 문제를 정확히 짚고 있는 항목.
- **조정해서 채택**: 방향은 맞지만 이 앱의 실제 구조(화면 개수, 톤, 기존 QA_LOG 원칙)에 맞게 수치·범위를 바꾼 항목.
- **보류/거절**: 이 세션에서 실행 불가능하거나(실제 3D 렌더링 에셋 제작), 이 앱의 다른 원칙과 충돌하거나, 굳이 지금 안 바꿔도 되는 항목. 12절에 별도로 정리했다.

**이 문서는 가이드 확정 단계다 — 아직 코드에는 반영하지 않았다.** 사용자가 이 문서를 검토·확정하면, 11절의 로드맵 순서대로 이어서 코드에 반영한다.

---

## 1. Design Vision

**"토스처럼 정돈되어 있는데, 핵심 그래픽엔 제작 리소스를 제대로 쓴 앱"** — 이 목표 인상을 그대로 채택한다. 참고로 사용자가 예시로 든 토스의 실제 브랜드 컬러도 파랑 계열이다(카카오뱅크·네이버 등도 마찬가지) — 이 앱이 이미 파랑 계열을 브랜드로 써온 것과 방향이 자연스럽게 맞는다(4절에서 상세히 다룬다).

구성 비율도 그대로 채택한다.

| 비율 | 영역 | 이 앱에서의 의미 |
|---|---|---|
| 80% | Clean UI | 카드, 버튼, 탭, 텍스트, 여백 — 지금 바로 손볼 수 있는 "시스템" 레이어 |
| 15% | Crafted Visual | 로또공, 번호 만들기 숏컷 4개 아이콘, 홈 히어로 — 제작 리소스가 필요한 레이어 |
| 5% | Brand Accent | 브랜드 색이 정확히 필요한 곳에만(CTA, 배지 등) 등장 |

---

## 2. 핵심 원칙 6가지

원안 6개 원칙은 전부 이 앱의 기존 원칙(QA_LOG 전반에 걸친 "과장 금지", §23 절제된 표현 원칙, 카지노/게임 느낌 회피)과 방향이 일치해 **그대로 채택**한다. 다만 ③번은 이 앱의 실제 화면 구조에 맞게 조정했다.

1. **UI는 UI답게** — 버튼·카드·탭에 억지로 입체감을 넣지 않는다. Depth는 정보 구조를 표현할 때만.
2. **3D는 장식이 아니라 Asset** — 3D(또는 3D처럼 보이는 제작물)가 들어가는 영역을 정확히 세 곳으로 제한: 로또공, 번호 만들기 숏컷 아이콘(현재 홈 화면 4개 퀵메뉴 + "번호 만들기" 탭 6개 카드 아이콘), 홈 히어로.
3. **한 화면에는 하나의 주인공만** — *(조정)* 이 앱의 홈 화면은 이미 이 원칙을 어느 정도 지키고 있다: 히어로 카드의 "AI로 번호 만들기" CTA가 1순위, 퀵메뉴 4개(제외해보기/행운번호/45면체/QR)가 2순위, "내가 자주 선택한 번호"·"최근 오래 나오지 않은 번호" 카드가 3순위 구조다. 다만 "번호 만들기" 탭(`generate.tsx`)은 6개 생성 모드가 리스트로 나열되고 그중 "AI 조합 탐색"에만 파란 "추천" 배지, "운명의 신"에 빨간 "HOT" 배지, "딥 패턴 탐색"에 보라 "NEW" 배지가 각각 다른 색으로 붙어 있어(코드에서 직접 확인: `#2563EB`/`#DC2626`/`#6C5CE7` 세 가지 색) 사실상 "주인공이 셋"처럼 보인다 — 10절에서 이 부분을 구체적으로 다룬다.
4. **컬러보다 여백으로 고급스러움을 만든다** — 여백 → Typography → Graphic → Color 순으로 디자인한다.
5. **같은 기능은 반드시 같은 언어를 사용** — 이 앱에 이미 이 원칙을 잘 지키는 사례(홈 화면의 "바로가기" 버튼 두 곳이 전부 `#0F172A` 하나로 통일)와 어기는 사례(위 3번의 배지 3색, 그리고 "브랜드 블루"라고 부를 수 있는 색이 실제로는 `#2563EB`/`#496DA3`~`#20385E`(그라디언트)/`#4338CA`(인디고 틴트) 세 갈래로 흩어져 있음)가 공존한다. 10절에서 정리한다.
6. **귀여움보다 세련됨** — 캐릭터·이모지·손글씨·별·하트 장식은 쓰지 않는다는 원칙은 이미 이 앱이 지키고 있다(현재 코드 전체에 이런 장식 요소 없음). Playful함은 로또공이 전담한다는 방향도 그대로 채택.

---

## 3. Visual Language

판단 기준 네 단어를 그대로 채택한다: **Clean / Precise / Soft / Crafted**. 반대로 실패로 간주할 인상도 그대로: Flashy / Cute / Gaming / Decorative. 로또라는 소재 특성상 카지노·게임 느낌으로 미끄러지기 쉽다는 지적도 정확하다 — 이 앱은 지금까지 QA_LOG에서 반복적으로 "당첨금 과시", "사행성 연상 문구"를 피해온 이력이 있어(GRAC 조사 때도 이 원칙이 실제로 도움이 됐다) 이 원칙과 일관된다.

---

## 4. Color System — 가장 중요한 결정 지점

### 지금 상태 (코드에서 직접 확인한 사실)

이 앱은 이미 "브랜드 블루" 계열을 쓰고 있지만, **하나의 토큰이 아니라 최소 4갈래로 흩어져 있다.**

| 값 | 쓰이는 곳 |
|---|---|
| `#2563EB` | 45면체 주사위 버튼, 선호번호 저장 버튼, 번호 만들기 "추천" 배지, 탭바 활성 색, 내 번호 탭 링크 텍스트 |
| `#496DA3` → `#20385E` (그라디언트) | 홈 히어로 "AI로 번호 만들기" CTA |
| `#0F172A` (다크 네이비) | 홈 "바로가기" 버튼, 결과 화면 "다시 생성" 버튼, 스플래시 배경, 헤더 배경 |
| `#4338CA` (indigo 틴트 fg) | 결과 화면 저장/구매예정/공유 버튼, 내 번호 탭 상태 배지 |

### 결정: 보라(Violet)로 바꾸지 않고, 지금의 파랑 계열을 하나로 정리한다 *(추천 — 사용자 확인 필요)*

GPT 원안은 브랜드 컬러를 `#5B4FF2`(Violet)로 제안했다. **이 부분은 채택하지 않는 것을 추천한다.** 이유:

1. 이 앱은 이미 파랑 계열을 수십 개 화면에 걸쳐 써왔다 — 지금 바꾸면 QA_LOG에 기록된 기존 색 관련 결정들(#13 다이스 버튼, #70 프레스 피드백 등)과 다시 전부 어긋나 위험이 크다.
2. 사용자가 직접 목표로 든 토스도 파랑이 브랜드 컬러다 — "정돈된 느낌"의 원천이 꼭 보라일 필요는 없다.
3. 원안 자체가 "Purple Gradient를 카지노·게임 느낌 회피 대상으로 조심하라"는 취지의 원칙(6절 "귀여움보다 세련됨", 3절 "Gaming 인상 회피")을 담고 있는데, 로또처럼 사행성 오해를 살 수 있는 서비스에서 보라색 그라디언트는 오히려 캐주얼 복권 앱·게임 앱에서 흔히 쓰이는 톤이라 원안의 다른 원칙과 살짝 충돌한다.

대신 **지금의 파랑 계열을 역할별로 정리해 하나의 토큰 세트로 통합**하는 걸 제안한다(실제 반영은 아직 안 함, 승인 시 `src/theme/colors.ts`에 추가):

| 제안 토큰 | 값(안) | 역할 |
|---|---|---|
| `brand.primary` | `#2563EB` | 표준 Primary 액션(버튼, 활성 탭, 강조 배지) — 지금 가장 넓게 쓰이는 값을 표준으로 승격 |
| `brand.pressed` | `#1D4ED8` | Primary 버튼 눌림 상태 |
| `brand.soft` | `#EFF6FF` | Primary Soft 배경(칩, 옅은 강조) |
| `brand.dark` | `#0F172A` | "항상 어두운" 히어로/CTA 전용(홈 히어로, 다시 생성 버튼 등 기존 관례 유지) |

CTA 그라디언트(`#496DA3`→`#20385E`)와 indigo 틴트(`#4338CA`)는 위 표준화 과정에서 `brand.primary`/`brand.dark` 중 하나로 흡수하거나, 의도적으로 다른 계열이 필요한 곳(예: 결과 화면 저장/공유 버튼)만 예외로 남기고 근거를 주석으로 남기는 걸 제안한다. **이 표 값은 확정이 아니라 제안 — 실제 반영 전 사용자 확인이 필요하다.**

### 유지: 배경/텍스트/서피스 토큰

`src/theme/colors.ts`의 배경·서피스·텍스트 3단계(Primary/Secondary/Muted)·테두리 토큰은 원안의 "Base/Text" 구성과 이미 거의 동일한 철학이라 **그대로 유지**한다. 원안이 제안한 `#F7F8FA`(배경)/`#171B24`(텍스트 Primary) 같은 구체적 수치도 지금 값(`#F8FAFC`/`#0F172A`)과 사실상 같은 톤이라 굳이 바꿀 실익이 적다.

### 추가 (2026-09-10, Phase 5c) — `brand` 토큰 실제 반영 완료 + `accentViolet` 신설

위 표의 `brand.primary`/`brand.dark` 제안은 이후 Phase 1~4·5c에 걸쳐 실제로 `src/theme/colors.ts`에 반영되어 전 화면(탭 허브 4개, `preferences.tsx`, `ErrorBoundary.tsx`, 스택 헤더 포함)의 `#2563EB`/`#1D4ED8`/`#0F172A` 하드코딩을 대체했다(`brand.soft`는 `tints.brand`로 이름만 다르게 구현). `app.json`의 스플래시 배경(`#0F172A`)은 JSON 설정 파일이라 토큰을 import할 수 없어 값만 `brand.dark`와 일치시키고 하드코딩으로 남겨뒀다.

별도로, "번호 만들기" 하위 두 기능(운명의 신·딥 패턴)이 brand와 무관하게 각자 다른 보라(`#7C3AED`/`#6C5CE7`)를 쓰던 것을 `accentViolet`(`primary: #7C3AED`, `light: #C4B5FD`) 하나로 통일했다 — 근거와 값 출처는 `colors.ts`의 `accentViolet` 선언부 주석 참고. `tints.purple`(배지·라벨용, 라이트/다크로 뒤집힘)은 별개 용도로 그대로 유지한다.

---

## 5. Typography

권장 서체(Pretendard)와 위계 표는 방향성은 **채택**하지만, **폰트 자체 도입은 별도 작업으로 분리**한다. 지금 앱은 `expo-font`가 이미 의존성에 있지만 실제로 커스텀 폰트를 로드하는 코드는 없다(`app/_layout.tsx` 확인 — `useFonts` 호출 없음, `assets/`에 폰트 파일도 없음). Pretendard를 실제로 쓰려면:

1. Pretendard 폰트 파일(OTF/TTF, 오픈소스) 확보 후 `assets/fonts/`에 추가.
2. `app/_layout.tsx`에서 `useFonts`로 로드하고, 로드 전엔 스플래시를 유지.
3. 앱 전체 `StyleSheet`에 흩어진 텍스트 스타일에 `fontFamily`를 붙여야 함(화면 수가 많아 일괄 작업 필요).

이 자체가 작지 않은 작업이라 **1차 디자인 개편(색·여백·카드·아이콘) 이후의 2차 작업으로 로드맵에 배치**했다(11절). 대신 **위계(Size/Weight) 규칙은 지금 시스템 폰트로도 바로 적용 가능**하므로 이번 개편에 포함한다 — 표는 원안을 그대로 채택.

| 용도 | Size | Weight |
|---|---|---|
| Hero | 30–32 | Bold 700 |
| Page Title | 24 | 700 |
| Section Title | 20 | 700 |
| Card Title | 17 | 600–700 |
| Body | 15 | 400 |
| Secondary | 14 | 400 |
| Button | 16 | 600 |
| Caption | 12–13 | 400 |

지금 코드는 카드 제목이 화면마다 14~17px로 들쑥날쑥하고(`generate.tsx` cardTitle 16, `index.tsx` cardTitle 14, `preferences.tsx` sectionTitle 15), Bold(700)를 캡션급 텍스트에도 종종 쓰는 경우가 있어(`setName` 13px에 `fontWeight: "700"`) 원안이 지적한 "Bold 남발" 문제가 실제로 관찰된다. 위계 표 적용 시 이런 부분부터 정리한다.

---

## 6. Spacing System

4px 그리드(4/8/12/16/20/24/32/40)를 **그대로 채택**한다. 코드를 직접 훑어본 결과, 대부분의 간격 값(8, 10, 12, 16, 20, 22, 24 등)은 이미 이 그리드에 가깝게 쓰이고 있어 원안과 방향이 잘 맞는다. 다만 그리드를 벗어나는 값도 실제로 발견된다 — 예: 홈 히어로 카드 `padding: 22`, 홈 카드 `padding: 14`, `설정 링크` `gap: 4`(그리드 안) vs 다른 곳 `gap: 6`. 전면 재작업보다는, **앞으로 새로 쓰는 값은 그리드 안에서만 고르고, 기존 값은 카드/버튼 시스템을 정리하는 김에(7절) 자연스럽게 그리드로 수렴**시키는 쪽을 추천한다.

화면 좌우 20px, Card ↔ Card 16px, Title↔Description 6~8px 등 세부 규칙도 그대로 채택 — 지금 코드의 실제 관례(`paddingHorizontal: 20`, `marginBottom: 16` 등)와 이미 상당히 가깝다.

---

## 7. Card Design

카드를 "존재감 있는 요소"가 아니라 "조용한 컨테이너"로 다루는 방향은 **채택**한다. 다만 두 가지를 실제 코드 기준으로 조정한다.

**Radius 통일**: 지금 코드에서 실제로 쓰이는 radius 값을 모으면 8, 9(shortcutButtonText 등 소소한 값 제외), 10(`DisclaimerCard`), 12(퀵메뉴 아이템, `chartPanel`, 제외번호 세트 카드), 14(로또 연구소 일부 카드), 16(홈 카드), 20(번호 만들기 탭 카드, 히어로 카드)으로 **최소 6종류가 혼재**한다. 원안처럼 "카드 radius가 전부 동일해야 고급스럽다"는 지적은 타당하다 — **2단계로 축소**하는 걸 제안한다: 인라인/보조 요소(칩, 배지, 작은 링크 카드) `12px`, 주요 콘텐츠 카드(홈 카드, 리스트 카드, 히어로) `20px`.

**Shadow 절제**: 원안 권장치(`0 2px 8px rgba(16,24,40,0.04)`)에 비해 `generate.tsx`의 카드 그림자(`shadowOpacity: 0.06, shadowRadius: 10, shadowOffset height 4`)가 조금 더 진하다. 완전히 다른 방향은 아니고 **소폭 절제** 수준의 조정이면 충분하다.

Border는 이미 전 화면이 `colors.border`(`#E2E8F0`) 1px로 통일돼 있어 **그대로 유지**. Glow, Gradient Card, Glass Card, 카드마다 다른 색 같은 금지 항목도 지금 앱에 해당 사례가 없어 그대로 원칙만 채택.

---

## 8. Signature 3D Art Direction — 이 세션에서 할 수 있는 것과 없는 것

**분명히 밝혀야 할 제약**: 이 세션은 코드/이미지 파일 편집만 가능하고, 실제 3D 렌더링(조명·재질·카메라 앵글을 갖춘 스튜디오급 3D 에셋) 파이프라인은 없다. 원안 8절(Material/Lighting/Reflection 공통 규격)은 **실제 3D 제작을 외주·전문 툴(Blender, Spline 등)로 진행할 때 제작 가이드로 그대로 쓸 것을 권장**하며, 이 문서에 규격을 보존해둔다(아래 "제작 시 참고 규격" 참고). 지금 세션에서 할 수 있는 건 코드 레벨 정리뿐이다.

**제작 시 참고 규격** (실제 3D 제작 착수 시 그대로 브리프에 사용):
- Material: Soft Polymer + Satin, Matte 75 / Gloss 25
- Lighting: 모든 오브젝트 10시 방향 Top-Left 광원 고정, 우하단 Soft Shadow
- Reflection: 작은 Soft Highlight만 허용, 큰 반사광·유리·메탈·크롬 금지

**지금 코드 상태 대비 현실적 진단**: 홈 화면 퀵메뉴 4개(제외해보기/행운번호/45면체/QR)와 번호 만들기 탭의 6개 카드 아이콘은 이미 `assets/quick-menu-icons/`의 커스텀 PNG로, "장식이 아니라 에셋"이라는 원안 방향과 이미 어느 정도 맞아 있다(단, 위 규격대로 만들어졌는지는 확인 못 함 — 이미지 자체를 열어 재질·광원을 대조하는 건 이 세션 범위 밖). 새로 제작한다면 4개(홈) 또는 6개(번호 만들기) 아이콘 전부를 위 공통 규격으로 다시 제작하는 것이 원안 취지에 가장 가깝다.

---

## 9. Lotto Ball

Shape/Highlight/Edge/Contact Shadow 등 형태 규격은 **3D 제작 착수 시 참고 규격으로 채택**한다(8절과 동일하게 실제 제작 없이는 반영 불가).

**색상은 원안을 그대로 따르지 않는 걸 제안한다.** 원안은 채도를 낮춘 색을 제안했지만, 이 앱의 `getBallColor()`는 이미 로또 공식 번호 색상 구간(1–10 노랑, 11–20 파랑, 21–30 빨강, 31–40 회색, 41–45 초록)을 정확히 따르고 있고, 이건 사용자가 실제 로또 용지·추첨 방송에서 매주 보는 색과 일치해야 하는 "인식 정확성"의 영역이다. 채도를 낮추면 고급스러워 보일 수는 있지만 "실제 로또공과 다르게 생겼다"는 혼란을 줄 위험이 있다 — **원색은 유지하되, 공 표면의 재질감(하이라이트·그림자)만 원안의 3D 규격을 따라 다듬는 정도**를 제안한다.

---

## 10. AI로 번호 만들기 CTA — 지금 상태와 조정안

| 항목 | 원안 | 지금 코드 | 제안 |
|---|---|---|---|
| 색 | Violet 그라디언트 | 네이비 그라디언트(`#496DA3`→`#20385E`) | 4절 결정에 따라 네이비/블루 계열 유지, 두 색상 정지점을 `brand.primary`/`brand.dark` 기반으로 재정의 |
| 높이 | 56px | `paddingVertical: 16`(실측 약 48~50px) | 56px로 소폭 확대 — 원안 취지(가장 중요한 액션이니 확실한 존재감) 채택 |
| Radius | 16–18px | 16px | 그대로 유지 |
| Shadow | `0 8px 20px rgba(91,79,242,.18)` | 없음(그라디언트 필만 있고 그림자 없음) | 색만 브랜드 블루 기준으로 치환해 추가 — CTA에만 약한 depth를 주는 건 1절 원칙(②)과도 맞음 |
| 문구 | "✦ AI로 번호 만들기 →" | "AI로 번호 만들기" (Sparkle·화살표 없음) | Sparkle 아이콘 추가는 채택 추천(AI 기능이라는 의미가 실제로 있어 원안 취지에 맞음), 화살표는 선택사항 |

---

## 11. 같은 기능은 같은 언어를 — 실제 발견한 불일치와 제안

코드를 직접 대조해 발견한 구체적 사례:

- **배지 3색 혼재**: `generate.tsx`의 "추천"(`#2563EB`)/"HOT"(`#DC2626`)/"NEW"(`#6C5CE7`) — 셋 다 "이 항목에 주목하라"는 같은 기능인데 색이 전부 다르다. 제안: 배지 유형을 실제로 구분해야 하는지 먼저 정하고(추천·신규·인기가 정말 다른 정보인지), 구분이 필요 없다면 하나의 톤(예: `brand.primary` 배경의 중립 배지)으로 통일, 구분이 꼭 필요하다면 색이 아니라 텍스트 라벨만으로 구분하는 방법도 검토.
- **"바로가기" 버튼은 이미 잘 통일돼 있음**: 홈 화면의 두 "바로가기" 버튼이 전부 `#0F172A`로 일치 — 이 부분은 손댈 필요 없이 다른 곳의 기준으로 삼으면 된다.
- **탭바 아이콘이 원안 17절과 정면으로 배치됨**: `app/(tabs)/_layout.tsx` 주석에 이미 "하단 탭 아이콘 전부 입체(글로시) 스타일 풀컬러 PNG로 통일했다"고 명시돼 있다. 원안은 정확히 이 패턴을 "하지 말아야 할 것"으로 지목한다("현재처럼 Bottom Nav 아이콘까지 3D로 만들지 않습니다... 그래야 위쪽 3D Asset이 더 비싸 보입니다"). **이건 이 문서 전체에서 가장 명확하고 실행 리스크가 낮은 권고다** — 새 3D 에셋 제작 없이 `@expo/vector-icons`(이미 의존성 있음, 다른 화면에서 Ionicons로 이미 광범위하게 사용 중)의 라인 아이콘으로 교체하면 되는 코드 레벨 작업이다. `tabBarActiveTintColor`/`InactiveTintColor` 로직은 이미 있는데 PNG라서 활용을 못 하고 있었던 것도 함께 해소된다.

---

## 12. 이번에 채택하지 않은 것 (보류/거절 목록)

| 항목 | 사유 |
|---|---|
| Violet(`#5B4FF2`)을 브랜드 색으로 전환 | 4절 참고 — 기존 파랑 계열과 충돌, 토스 레퍼런스와도 안 맞음. 사용자가 원하면 재검토 가능 |
| 로또공 채도 낮추기 | 9절 참고 — 공식 색상 인식 정확성이 우선 |
| 실제 3D 렌더링 에셋 즉시 제작 | 이 세션은 3D 렌더링 파이프라인이 없음. 규격만 문서화, 실제 제작은 외주/전문 툴 필요 |
| Pretendard 폰트 즉시 적용 | 폰트 파일 확보 + `useFonts` 통합 + 전 화면 스타일 수정이 필요한 별도 작업. 위계 규칙(5절)만 먼저 적용 |

---

## 13. Premium Detail — 다음 시각 QA 때 쓸 체크리스트

원안 19절의 디테일 항목들을 이 앱 기준의 실제 QA 체크리스트로 채택한다. 색·간격·카드를 1차로 정리한 뒤, 실기기 확인 시 아래 항목을 훑어본다.

- 로또공 6개의 그림자 방향이 전부 동일한가
- 숏컷 아이콘(퀵메뉴 4개, 번호 만들기 6개)의 카메라 앵글·명암이 서로 같아 보이는가
- 화살표(`chevron-forward` 등) 크기가 화면마다 같은가
- 카드 radius가 12/20 두 값으로만 쓰이는가(7절)
- 버튼 텍스트의 세로 정렬이 깨지지 않는가
- 회차별 숫자가 로또공 정중앙에 위치하는가
- 섹션 간 수직 리듬(32px 등)이 화면마다 일정한가

---

## 14. 반영 로드맵 (다음 단계)

이 가이드를 확정하면 아래 순서로 진행하는 걸 제안한다 — 파운데이션(위험 낮음)부터 시작해 점진적으로 확장한다.

1. **Phase 1 — 토큰 정리** (`src/theme/colors.ts`에 `brand.*` 토큰 추가, 4절 표 확정)
2. **Phase 2 — 탭바 아이콘 2D 전환** (11절 — 새 에셋 제작 없이 바로 가능한 가장 임팩트 큰 변경)
3. **Phase 3 — 카드 radius/shadow 통일** (7절 — 홈, 번호 만들기, 로또 연구소, 내 번호 순으로 화면별 적용)
4. **Phase 4 — 배지·CTA 색 정리** (10, 11절)
5. **Phase 5(선택)** — Pretendard 폰트 도입
6. **Phase 6(선택, 외주 필요)** — 로또공·숏컷 아이콘 실제 3D 재제작

각 Phase는 QA_LOG에 별도 항목으로 기록하고, 이 세션이 화면을 직접 볼 수 없는 만큼 Phase마다 사용자의 실기기 확인을 거친 뒤 다음 Phase로 넘어가는 걸 권장한다.

---

## 15. 화면 수정 전 필수 게이트 — Design Principle 체크리스트 (2026-09-10 확정)

문서 맨 위에 고정한 **"Quiet UI. Crafted Objects."** 원칙을 실제 작업에서 놓치지 않도록, 화면 하나를 수정·리뷰할 때마다 아래 6문항을 통과해야 한다. 하나라도 "아니오"면 그 수정은 다시 검토한다 — 13절(Premium Detail 체크리스트)이 "디테일이 맞는가"를 보는 더 세밀한 단계라면, 이 15절은 그보다 먼저, 더 큰 단위에서 "이 방향 자체가 맞는가"를 거르는 게이트다.

1. **더 화려해졌는가가 아니라 더 정돈됐는가?** — 요소를 더했다면 그만큼 다른 무언가를 뺐는지 확인한다.
2. **UI보다 콘텐츠가 먼저 보이는가?** — 카드·버튼·배지 장식이 번호·결과·금액 같은 실제 정보보다 시선을 더 끌면 실패다.
3. **3D가 장식이 아니라 제작 Asset처럼 보이는가?** — 8절에서 정한 3D 허용 영역(로또공·숏컷 아이콘·홈 히어로) 밖에 입체 효과를 새로 넣지 않았는지, 그 영역 안에서도 그림자·하이라이트가 즉흥적 장식이 아니라 의도된 제작물처럼 일관되는지 본다.
4. **다른 화면과 같은 앱처럼 보이는가?** — radius(7절)·색 토큰(4절)·타이포(5절) 기준이 이 화면에도 동일하게 적용됐는지 확인한다.
5. **Gradient와 Shadow를 제거해도 디자인이 성립하는가?** — 그라디언트·그림자를 걷어냈을 때 레이아웃과 위계가 무너진다면, 그 효과가 구조가 아니라 장식으로 구조를 대신하고 있다는 신호다.
6. **20~30대가 봤을 때 로또/카지노 사이트가 아니라 잘 만든 Product App처럼 보이는가?** — 3절(Visual Language)의 "Flashy/Cute/Gaming/Decorative 회피" 원칙을 최종적으로 다시 확인하는 문항이다.

이 6문항은 향후 모든 Phase·모든 화면 수정에 기본으로 적용한다 — 사용자가 매번 다시 요청하지 않아도 이 문서를 참조하는 모든 작업에 자동으로 적용되는 게이트다.
