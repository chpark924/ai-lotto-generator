# 금손로또 종합 재점검 리포트 (2026-08-08)

시니어 개발자 · 보안책임자 · 시니어 기획자 · 시니어 UI/UX 디자이너 관점에서, `APP_REVIEW_2026-08-06.md`(직전 리포트) 이후 쌓인 QA_LOG #49~#57(8/6~8/8, 9개 항목)을 코드 레벨로 직접 재검증했다. 이번 사이클의 핵심은 신규 기능 "딥 패턴 탐색"(§7 Multi-scale basin, §8 kNN Geometric Void, §14 Null 시뮬레이션+다중검정 보정)의 통계 엄밀성 강화, 체감 대기시간 최적화, `components`(jest-expo) 렌더링 테스트 신규 도입이다. `npx tsc --noEmit` / `npx eslint .` / `npx jest --selectProjects unit`(25개 스위트 205개 테스트) / `components` 프로젝트(딥 패턴 화면 3개, 11개 테스트 개별 실행 확인) / `npm audit --omit=dev`를 실제로 실행했고, 이 재점검 과정에서 딥 패턴 화면 2곳의 실결함(다크모드 대비 미달, 내부 버전 문자열 노출)을 직접 찾아 그 자리에서 수정까지 반영했다.

## 종합 점수 비교

| 영역 | 8/6 점수 | 8/8 재점검 | 변화 |
|---|---|---|---|
| 보안 | 92 | **93** | ▲ 1 |
| 안정성 | 87 | **90** | ▲ 3 |
| 최적화 | 86 | **91** | ▲ 5 |
| 사용자 편의성(UX/UI) | 89 | **92** | ▲ 3 |
| 경쟁 앱 대비 강점 | 84 | **87** | ▲ 3 |
| **종합** | **~88** | **~91 / 100** | ▲ 3 |

8/6 이후 이틀간의 작업은 대부분 "딥 패턴 탐색" 한 기능에 집중됐다: 정직하게 "아직 안 했다"고 명시해뒀던 kNN 보정과 Null 시뮬레이션/다중검정 보정을 실제로 구현해 통계적 과장을 걷어냈고(#54), 이어서 basin 해상도를 3단계로 세분화하면서 체감 대기시간의 구조적 병목(런타임 rejection sampling)을 근본적으로 제거했다(#55, #56). 8/6 리포트의 "중기 개선" 4개 항목(글씨 배율 잘림, 회전 대응, 태블릿 여백, splash 플러그인 이전)도 이 사이의 별도 세션에서 전부 반영된 상태임을 이번에 코드로 재확인했다. 이번 재점검 자체에서도 다크모드 텍스트 대비 미달과 엔진 내부 버전 문자열이 사용자 화면에 그대로 노출되던 문제를 발견해 즉시 수정했다 — 8/6 리포트가 언급한 "근접 사고를 검증 프로세스가 걸러낸" 흐름이 이번에도 반복됐다.

---

## 1. 시니어 개발자 관점 — 안정성 · 코드 품질

### 확인한 강점
- `npx tsc --noEmit`, `npx eslint .` 이번 재점검에서도 클린(딥 패턴 신규/수정 파일 포함 전체 리포지토리 기준). `npx jest --selectProjects unit` **25개 스위트 205개 테스트 전부 통과**(8/6 시점 20개 스위트 168개에서 순증, 딥 패턴 관련 신규 스위트 5개 반영).
- **"정직한 근사치 → 하나씩 붙여나가기" 원칙이 실제로 지켜졌다**(#54): 이전 세션에서 Atlas의 `methodology` 필드에 "아직 안 함"이라고 명시해뒀던 kNN Geometric Void 보정과 Null 시뮬레이션+다중검정 보정을, 이번에 실제로 구현하고 정직한 결과를 그대로 반영했다. 특히 family-wise 보정 결과 "구조적 공백 HIGH" 81개 basin 중 통계적으로 유의한 것은 1~3개뿐이라는, 겉보기보다 훨씬 보수적인 결과가 나왔는데도 임계값을 조정해 "더 그럴듯하게" 만들지 않고 그대로 반영한 점이 이 프로젝트의 §39(과장 금지) 원칙과 일치한다.
- **체감 대기시간 문제를 원인 진단부터 구조적으로 해결**(#55): 기존 `sampleFromBasin()`이 CSPRNG로 무작위 조합을 뽑고 목표 basin 소속 여부를 다시 계산해 버리는 rejection sampling이었는데, 81개 basin 중 하나에 맞을 확률이 약 1.2%라 후보 풀을 채우려면 basin당 최대 3,000회의 `expo-crypto` 브릿지 호출이 필요한 구조였다. 이를 빌드타임 reservoir sampling(Algorithm R, 결정적 seed)으로 옮겨 basin당 런타임 CSPRNG 호출을 20회 안팎으로 줄였다 — 통계적 성질(basin 내부 균등 무작위 표본)은 그대로 유지하면서 병목만 제거한 설계가 합리적이다.
- **파일 무결성 검증 과정에서 자체적으로 발견한 정밀도 버그를 정직하게 추적**(#55): Atlas 재빌드 후 12,150개 대표 후보의 basin 소속을 교차검증하다 1,935건 불일치가 나왔는데, 원인을 끝까지 추적해 "빌드 스크립트는 Float32Array로 좌표를 저장하는데 검증 스크립트는 Float64로 재계산해서 생긴 오탐"임을 밝히고(`Math.fround()`로 재현) 0건으로 해소했다. 이 과정에서 "기존 런타임 엔진도 같은 float32/float64 어긋남 위험을 안고 있었다"는 잠재 버그를 부수적으로 찾아 리팩터로 함께 없앤 점이 꼼꼼하다.
- **hot path 낭비 제거가 근거 있게 이뤄짐**(#56): `getPaperPosition()`을 매 호출 재계산에서 1~45 조회 테이블로, `kNearestVoidScore()`의 candidate 중복 정렬(이력 1,235건 순회마다 반복)을 진입 시 1회 정렬로 교체 — 둘 다 "결과가 항상 같은 순수 계산의 중복 제거"라는 안전한 범주의 최적화이며, 반환 객체 공유가 안전한지(호출부에서 mutate 여부) 직접 확인한 뒤 적용한 점도 합리적이다.
- **`components`(jest-expo) 렌더링 테스트가 이번에 처음 정착**(#57): 8/4 리뷰에서 지적됐던 "순수 함수는 멀쩡한데 화면 배선이 끊긴 버그(`lab.tsx` dead-code)" 유형의 사각지대를 메우기 위해, 딥 패턴 화면 3개(소개/결과/상세)에 대해 버튼 클릭 → 엔진 호출 → 스토어 저장 → navigate, 저장 버튼 → `saveTicket` → `Alert` 흐름을 실제 렌더링으로 검증하는 테스트 11개를 신규 추가했다. 이 과정에서 `jest.mock()` 호이스팅 스코프 규칙, `react-native-safe-area-context` 공식 mock의 순환 참조 버그(moduleNameMapper와 `jest.requireActual` 상호작용 문제) 2건을 실제로 만나 근본 원인까지 추적해 해결했고, 그 인프라(`tests/mocks/react-native-svg.tsx`, `tests/mocks/safe-area-context.tsx`)는 향후 다른 화면 테스트에도 재사용 가능하다.
- **이번 재점검 자체에서 발견·수정한 결함 2건**: (1) `deep-pattern-detail.tsx`의 `vizTitle`과 `deep-pattern-result.tsx`의 `basinTag`가 다크모드 배경(`colors.surface`, `#161F32`) 위에서 고정값 `#5847D6` 텍스트를 써 대비비 약 2.6:1로 WCAG AA(4.5:1) 미달이었다 — 이미 프로젝트에 정의돼 있던 `tints.purple.fg`(라이트 `#5B21B6`/다크 `#DDD6FE`, 둘 다 AA 통과)로 교체했다. (2) `deep-pattern-result.tsx`의 하단 안내 문구가 `engineVersion`/`atlasVersion`(예: `"DPE-1.1-v3approx"`) 내부 식별자를 그대로 사용자 화면에 노출하고 있었다 — `src/lib/deepPattern/types.ts` 자체의 설계 원칙("엔진 내부 개념은 이 타입 밖으로 나가지 않는다")과 배치되고 앱의 다른 어떤 화면에도 없는 패턴이라, 회차 정보만 담은 일반 문구로 교체했다. 두 수정 모두 `tsc`/`eslint` 클린, 해당 컴포넌트 테스트(`deepPatternDetail.test.tsx` 4/4, `deepPatternResult.test.tsx` 4/4) 재실행으로 회귀 없음을 확인했다.

### 새로 확인한 리스크
- **딥 패턴 엔진의 실기기 latency는 여전히 미실측이다.** #55~#56에서 구조적 병목(런타임 rejection sampling, 중복 정렬/재계산)을 근거 있게 제거했고 Node/Jest 환경에서 `recommend(5)`가 약 400ms 안팎(가드레일 테스트 기준 3초 이내)으로 끝나는 것을 확인했지만, 이는 데스크톱 V8 기준이라 실기기 Hermes 엔진 성능을 대변하지 않는다. 사용자가 실기기 테스트를 EAS 빌드 쿼터 문제로 의도적으로 뒤로 미룬 상태라 이 항목은 다음 실기기 테스트 세션에서 반드시 1순위로 확인해야 한다.
- **`components` 프로젝트가 이 샌드박스 환경에서 여전히 불안정하다.** 이번 재검증 중 `lab.test.tsx`(딥 패턴과 무관한 기존 테스트)가 "5000ms 테스트 타임아웃 초과"로 실패하는 것을 관찰했다 — 8/6 리포트 이후 처음 발견된 현상은 아니고(QA_LOG #57에 이미 유사 타임아웃이 기록돼 있고 사용자 로컬 `npm test`에서는 전부 통과가 확인된 이력이 있음), 이번에도 딥 패턴 화면 2개(`deepPatternDetail`, `deepPatternResult`)를 개별 실행해서는 각각 정상 통과(4/4, 4/4)를 확인했다. 즉 코드 결함이라기보다 이 세션 환경 특유의 콜드스타트/누적 부하 문제로 보이지만, **100% 확신할 수는 없으므로 사용자가 로컬에서 전체 `npm test`를 한 번 더 돌려 `lab.test.tsx`가 여전히 정상인지 확인하는 것을 권장한다.**
- New Architecture 전환 계획(8/6 리포트에서 지적, SDK 55부터 `newArchEnabled: false` 무시됨)은 이번 사이클에서 다뤄지지 않았다 — 지금 당장 급한 사안은 아니지만 계속 이월되고 있다.

**점수: 90/100** (8/6 87 대비 +3 — "정직한 근사치 → 하나씩 완성" 원칙을 스스로 지켜가며 통계 엄밀성을 실제로 채워 넣은 점, 정밀도 버그를 끝까지 추적해 해소한 점, 처음으로 화면 렌더링 테스트 인프라를 구축한 점이 명확한 개선. 실기기 latency 미실측과 `components` 프로젝트의 샌드박스 특유 불안정성이 남아 있어 만점은 아님)

---

## 2. 보안책임자 관점

### 재확인한 강점
- **딥 패턴 기능이 서버 없음 원칙을 그대로 유지했다.** Atlas는 빌드타임에 생성되는 정적 JSON(`data/deep-pattern-atlas.json`, 312.3KB)이고 런타임에 네트워크 호출이 전혀 없다 — 새 기능이 추가됐음에도 앱의 위협 표면(attack surface)이 늘지 않았다는 뜻이다.
- **`package.json` diff가 정확히 비어 있다** — 8/6 리포트 이후 지금까지 딥 패턴 기능 전체(kNN 보정, Null 시뮬레이션, basin 세분화, reservoir sampling, `components` 테스트 인프라)를 신규 npm 패키지 하나 없이 기존 의존성(`expo-crypto`, `jest-expo`, `@testing-library/react-native` 등 이미 있던 것)만으로 구현했다. 공급망 위험이 늘지 않았다.
- 하드코딩된 API 키·시크릿·토큰 재스캔 결과 없음(`app/`, `src/`, `scripts/` 전체 정규식 스캔, 이번 재점검에서 재실행).
- 딥 패턴 기능은 사용자의 개인 데이터(생년월일, 저장 이력 등)를 전혀 참조하지 않는다 — 순수하게 정적 Atlas와 CSPRNG만으로 동작해, 프라이버시 관점에서 검토할 새 표면이 없다.

### 재확인한 사항 (변동 없음)
- `npm audit --omit=dev` 재실행 결과 **25건(moderate 11, high 14)으로 8/6(15건)보다 증가**했다. 원인을 diff로 확인한 결과 이번 사이클의 코드 변경과는 무관하며, 여전히 `@expo/config-plugins → xcode → uuid` 체인(및 이를 참조하는 `expo-constants`/`expo-asset`/`expo-linking`/`expo-notifications`/`expo-router`/`expo-splash-screen` 상위 패키지들)에서 새로 게시된 upstream advisory가 반영된 것으로 판단된다 — `grep`으로 재확인한 결과 이 체인의 패키지들은 EAS 빌드/prebuild 툴체인 전용이며 `app/`·`src/` 런타임 코드에서 직접 import하는 곳이 없다. 건수 증가 자체를 방치할 사안은 아니지만, 지금 당장 앱 런타임 보안에 영향을 주는 취약점은 아니라는 8/6 리포트의 결론이 이번에도 유효하다.
- dhlottery.co.kr/GitHub raw 요청에 인증서 고정 없음, 나머지 설정값(선호번호, 알림 시각 등)이 AsyncStorage 평문인 부분은 8/6과 동일하게 의도적 트레이드오프로 재확인, 재지적하지 않는다.

**점수: 93/100** (8/6 92 대비 +1 — 대규모 신규 기능을 추가하면서도 공격 표면과 의존성을 전혀 늘리지 않은 설계 판단이 소폭 플러스. `npm audit` 건수 증가는 코드 변경과 무관함을 확인했으나 건수 자체가 계속 늘고 있어 다음 사이클에 한 번은 EAS 툴체인 업데이트 검토가 필요하다는 점에서 만점은 아님)

---

## 3. 시니어 기획자 관점 — 사용자 편의성 · 정합성

### 이번에 확인한 개선
- **딥 패턴 탐색이 명세(§39 과장 금지)를 지키며 완성도를 높여갔다.** kNN 보정과 Null 시뮬레이션이 추가되면서 "구조적 공백"이라는 표현의 근거가 훨씬 탄탄해졌지만, 동시에 "대부분의 basin은 무작위 변동과 통계적으로 구분되지 않는다"는 정직한 결과도 그대로 화면(통계적 유의성 지표 + "낮음도 정상"이라는 캡션)에 반영했다. 당첨확률 자체를 높인다는 오해를 줄 수 있는 어떤 수정도 없었다.
- **8/6 리포트의 "중기 개선" 4개 항목이 전부 반영된 상태임을 git 로그와 코드로 재확인했다**: 로또 공/배지 텍스트 `maxFontSizeMultiplier` 적용, `SettingsSheet.tsx`의 `useWindowDimensions()` 전환(회전 대응), `NumberGrid.tsx` 화면폭 반응형 셀 크기, `app.json` splash 설정의 `expo-splash-screen` 플러그인 방식 이전까지 — 8/6 이후 별도 세션에서 이미 마무리됐다. "공식 사이트에서 구매하기" 링크 추가, `tab-icon-home.png` 리사이즈(859×859·204KB → 256×256·33KB)도 함께 확인했다.
- **체감 대기시간 개선이 기획적으로 중요한 이유**: 사용자가 명시적으로 "너무 길면 이탈이 우려된다"고 지적한 지점을 실기기 실측 없이도 구조 진단(rejection sampling의 확률적 비효율)으로 원인을 특정하고 근본적으로 손을 댄 순서가 합리적이다 — 이탈 위험이 있는 기능을 "일단 실기기에서 느려 보이면 그때 고치자"가 아니라 사전에 구조적으로 막은 접근.

### 남아있는 정합성/편의성 격차
- 딥 패턴 탐색의 실제 체감 속도는 여전히 실기기에서 확인된 바 없다 — 다음 실기기 테스트 세션에서 "패턴 분석 시작하기" 버튼을 눌렀을 때 대기시간이 실제로 개선됐는지가 이 기능의 최종 성패를 가른다.
- New Architecture 전환 계획은 8/6 이후에도 아직 다뤄지지 않았다(장기 과제로 재이월).

**점수: 92/100** (8/6 89 대비 +3 — 신규 기능의 명세 준수와 8/6 리포트 지적 사항의 완전한 반영이 근거. 실기기 latency 확인이 아직 남아 있어 만점은 아님)

---

## 4. 시니어 UI/UX 디자이너 관점

### 확인한 강점
- **이번 재점검에서 직접 발견·수정한 접근성/일관성 결함 2건**: 다크모드 텍스트 대비 미달(위 1장 참고)과 내부 엔진 버전 문자열의 UI 노출을 그 자리에서 고쳤다. 두 문제 모두 "화면은 정상적으로 렌더링되고 기능도 동작하지만 세부 마감이 어긋난" 유형이라 자동 테스트만으로는 잡히지 않았고, 코드를 직접 읽고 계산(WCAG 대비비 수식)하는 리뷰 과정에서만 드러난다는 점에서 이런 종합 재점검의 존재 가치를 보여준다.
- **딥 패턴 상세 화면의 지표 행에 접근성 그룹화를 추가**했다(이번 재점검에서 반영): 기존에는 라벨/값/점 3개 요소를 스크린리더가 따로 읽어 "구조적 공백... 높음... 점..."처럼 끊겨 들렸는데, 행 전체를 하나의 접근성 요소로 묶어 "구조적 공백: 높음"으로 한 번에 읽히도록 했다. 8/6 리포트가 "접근성 커버리지가 넓다"고 평가했던 `accessible + accessibilityLabel` 그룹화 패턴(결과 카드 등)을 새 화면에도 동일하게 적용한 것이라 일관성이 유지된다.
- **딥 패턴 탐색 소개 화면(`deep-pattern.tsx`)이 accessibilityLabel 없이 보이는 것은 오탐이었다**: 파일 자체에는 `accessibilityLabel` 문자열이 없지만, 화면의 유일한 인터랙션(하단 CTA)이 공용 컴포넌트 `BottomActionBar`를 통하고 이 컴포넌트가 내부적으로 `label` prop을 `accessibilityRole="button"`/`accessibilityLabel`로 자동 설정한다 — grep만으로 판단했다면 놓쳤을 부분이라 실제 컴포넌트 코드까지 열어 확인했다.
- 색상 사용 일관성(초록 톤 배지 통일, `tints.purple`의 사용처가 딥 패턴 전용으로 국한됨)은 8/6 상태 그대로 유지되고 있음을 재확인했다.

### 남아있는 사항
- 다크모드 수동 토글 부재(시스템 설정만 추종)는 8/6과 동일하게 낮은 우선순위로 남아있다.
- New Architecture 대응, edge-to-edge 실기기 육안 확인은 UI/UX 관점에서도 여전히 미확인 상태다.

**점수: 92/100** (8/6 89 대비 +3 — 이번 재점검 자체에서 접근성·대비 결함을 발견해 수정까지 완료한 것이 핵심 근거. 다크모드 수동 토글, 실기기 육안 확인 등 낮은 우선순위 항목이 남아 있어 만점과는 거리가 있음. 기획자 관점과 통합해 위 종합표에서는 "사용자 편의성(UX/UI)" 한 줄로 표기)

---

## 5. 경쟁 앱 대비 포지션

8/6 리포트 이후 별도의 경쟁 앱 재조사는 진행하지 않았다(이번 사이클은 신규 기능 완성도에 집중됐다) — 아래는 8/6 조사 결과에 이번 사이클의 변화만 반영한 것이다.

### 경쟁력 있는 부분
- **"딥 패턴 탐색"이 8/6 리포트가 언급했던 "전문 분석 배지"보다 한 단계 더 나아간 차별점으로 완성됐다**: 단순 배지 표시가 아니라, 814만 개 전체 조합 공간을 실제로 전수 분석하고, Multi-scale basin(Fine/Mid/Coarse) + kNN 기하학적 보정 + Null 시뮬레이션 기반 다중검정 보정까지 갖춘 구조는 이번에 조사한 국내 인기 로또 앱 어디에서도 흔히 보이지 않는 통계적 엄밀성이다. 동시에 §39 원칙(당첨확률과 무관함을 매 화면에 명시)을 지켜 "그럴듯하지만 과장된 마케팅"으로 흐르지 않은 점이 신뢰도 측면의 차별점이다.
- 8/6 리포트에서 확인된 QR 당첨확인, 통계 깊이, 온디바이스 알림 등의 경쟁력은 이번 사이클에서 변경되지 않아 그대로 유지된다.

### 남은 격차
- 딥 패턴 탐색의 실기기 체감 속도가 아직 검증되지 않아, "통계적으로는 탄탄하지만 느리게 느껴지는 기능"이 될 위험이 완전히 해소됐다고 단정할 수는 없다 — 이 부분이 해소되면 차별점의 설득력이 한층 강해진다.
- 판매점 찾기, 공식 구매 페이지 연결 이후 추가 여지(예: 구매 리마인드와 딥 패턴 결과 연동) 등은 8/6과 동일하게 낮은 우선순위로 남아있다.

**점수: 87/100** (8/6 84 대비 +3 — 딥 패턴 탐색이 배지 수준을 넘어선 실질적 통계 차별점으로 완성된 것이 근거. 이번 사이클엔 경쟁 앱 재조사를 하지 않았고 실기기 검증도 남아있어 만점과는 거리가 있음)

---

## 우선순위별 개선 제안

**빠른 저비용 개선 (Low effort / 체감 효과 높음)**
1. `components` 프로젝트를 사용자 로컬에서 전체 실행(`npm test`)해 `lab.test.tsx`가 이번 세션의 샌드박스 타임아웃과 무관하게 정상 통과하는지 최종 확인.
2. `data/deep-pattern-atlas.json`(312.3KB)이 커밋에 포함돼 있는지, `.gitignore`/빌드 산출물 정책과 맞는지 한 번 점검(이미 리포에 포함하는 것이 의도된 설계이나, 다음에 `sampleCombos` 크기를 늘릴 경우 파일 크기가 계속 늘어날 수 있다는 점을 염두에 두면 좋다).

**중기 개선**
3. `npm audit` 건수가 8/6(15건) → 8/8(25건)로 증가한 원인이 된 `@expo/config-plugins` 체인의 upstream 패치 여부를 다음 SDK 패치 사이클에서 한 번 확인.
4. 딥 패턴 탐색 실기기 latency 실측 후, 필요하면 `kNearestVoidScore`의 top-k 선택 방식이나 `CANDIDATE_POOL_SIZE`/`sampleCombos` 크기를 조정(QA_LOG #56에 이미 후보로 기록돼 있음).

**장기 계획 (지금 당장 급하지 않으나 미리 인지해야 함)**
5. New Architecture 전환 계획 수립 — 8/6 리포트에서 지적된 사안이 이번 사이클에도 다뤄지지 않아 계속 이월되고 있다.
6. Android edge-to-edge(API 36) 실기기 육안 확인 — 8/6과 동일하게 아직 미확인.
7. 실기기 테스트 세션에서는 딥 패턴 탐색의 체감 속도를 최우선으로 확인할 것(사용자가 이미 이 순서로 계획 중).

---

## 검증 방법 및 한계

이 리포트는 코드 정적 분석(딥 패턴 관련 신규/수정 파일 전체 및 관련 컴포넌트 직접 읽기), `npx tsc --noEmit`(클린), `npx eslint .`(클린), `npx jest --selectProjects unit`(25개 스위트 205개 테스트 통과), `components` 프로젝트에서 이번 재점검이 직접 수정한 두 화면(`deepPatternDetail.test.tsx` 4/4, `deepPatternResult.test.tsx` 4/4) 개별 실행 확인, `npm audit --omit=dev`(25건, moderate 11/high 14, 8/6 대비 증가하나 코드 변경과 무관함을 diff로 확인), git 로그 대조를 근거로 작성했다. WCAG 대비비는 sRGB 상대 휘도 공식으로 수동 계산했다(자동화된 대비 검사 도구는 사용하지 않았다). 경쟁 앱 재조사, 실기기 사용성 테스트(딥 패턴 latency, 폰트 배율, edge-to-edge), 스토어 등록 전 최종 QA는 이 사이클에서 다루지 않았다 — 사용자가 실기기 테스트를 EAS 빌드 쿼터 문제로 별도 세션으로 미루기로 결정한 것과 일관된다. `components` 프로젝트 전체(5개 파일)를 한 번에 실행하는 것은 이 세션 환경의 도구 호출 시간 제한 때문에 이번에도 하지 못했다 — 개별 파일 실행으로 대체했다는 점을 명시해둔다.
