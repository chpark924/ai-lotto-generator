# 금손로또 종합 재점검 리포트 (2026-08-26)

시니어 개발자 · 보안책임자 · 시니어 기획자 · 시니어 UI/UX 디자이너 관점에서, `APP_REVIEW_2026-08-13.md`(직전 리포트) 이후 2주간 쌓인 QA_LOG #113~#117(5개 항목)을 코드 레벨로 재검증했다. 이번 사이클은 8/13 리포트가 세 번째로 이월시킨 "대량 UI 변경의 실기기 미검증" 국면 이후에 진행된 것으로, 성격이 사뭇 다르다. 실기기에서 실제로 발견된 버그를 근본 원인까지 추적한 항목이 다수이고(#114 미래 회차 오표시, #116 CI 3연속 실패), 새 기능 1건(#113 기대 대비 실제 1등 당첨자 수 카드)과 사용자 피드백 기반 정밀 조정 1건(#117 카드 간격)이 더해졌다. 이 리포트는 또한 QA_LOG 항목 재검토에 그치지 않고, 이 세션이 원격 기기 브릿지로 직접 `npx tsc --noEmit`/`npx eslint .`/`npm audit`/`git status`/실제 당첨 데이터 파일을 재실행·재확인한 결과를 근거로 삼는다 — 이전 두 리포트가 "각 QA_LOG 항목에 기록된 결과를 재해석"하는 방식이었다면, 이번엔 그 위에 이 리포트 작성 시점의 독립적인 재검증을 한 겹 더했다.

이와 별도로, 이번 사이클에는 코드 변경 이외의 두 갈래 작업이 함께 있었다. 하나는 구글 플레이스토어 실제 출시 절차를 이 프로젝트의 `app.json`/`eas.json` 실제 설정과 대조해 정리한 것이고, 다른 하나는 GRAC(게임물관리위원회) 등급분류가 실제로 필요한지를 법적 정의·Google 공식 정책·실제 경쟁 앱 사례 세 갈래로 조사한 것이다. 둘 다 코드 변경은 아니지만 "출시가 가능한 상태인가"라는 질문에 직접 답하는 작업이라, 이번 리포트의 별도 절(6절)로 다룬다. 또한 프로젝트에 별도 보관 중인 "판매점/1등 배출점 지도 기능" 아키텍처 제안서(2026-08-19, 사용자 결정으로 보류)도 로드맵 항목으로 짚는다.

## 종합 점수 비교

| 영역 | 8/13 재점검 | 8/26 재점검 | 변화 |
|---|---|---|---|
| 보안 | 93 | **94** | ▲ 1 |
| 안정성 · 코드 품질 | 91 | **91** | – |
| 최적화 | 91 | **91** | – |
| 사용자 편의성(UX/UI) | 93 | **94** | ▲ 1 |
| 경쟁 앱 대비 강점 | 88 | **89** | ▲ 1 |
| **종합** | **~91** | **~92 / 100** | ▲ 1 |

이번 사이클의 점수 변화가 크지 않은 이유를 먼저 밝힌다. #114(미래 회차 오표시)와 #116(CI 3연속 실패)은 둘 다 "실기기·실사용에서 실제로 터진 문제를 근본 원인까지 짚어 고쳤다"는 점에서 질적으로 좋은 대응이었지만, 뒤집어 보면 두 문제 모두 상당 기간 사용자가 직접 겪거나(#114는 스크린샷으로 재차 신고) CI 이메일을 받고 나서야(#116) 발견됐다는 뜻이기도 하다. 여기에 이번 리포트 작성 중 새로 발견한 git 작업 트리의 줄바꿈 문자(CRLF/LF) 불일치(아래 1절 참고)가 더해져, "고친 것"과 "새로 확인된 위생 문제"가 서로 상쇄한다.

---

## 1. 시니어 개발자 관점 — 안정성 · 코드 품질

### 확인한 강점

- **#116(TRANSITION_FREQUENCY_NOTICE 누락)의 원인 규명이 정확하다.** 이 상수는 이번 세션 이전부터 `lab.tsx`와 `drawStats.ts` 문서 주석에서 이미 참조되고 있었는데 실제 `export const`가 `messages.ts`에 없었다 — Metro/Expo 개발 서버는 존재하지 않는 named import를 런타임에서 `undefined`로 조용히 통과시키므로 로컬 개발 중에는 전혀 드러나지 않고, CI의 `tsc --noEmit`만 이를 잡아낸다는 메커니즘까지 정확히 짚었다. 고친 뒤 다른 3개 화면(lab.tsx, index.tsx, ai-search.tsx, exclusion.tsx)이 `constants/messages`에서 가져오는 모든 `*_NOTICE` import에 대해 실제 export 존재 여부를 전수 grep으로 재확인한 것도 "같은 클래스의 문제가 다른 곳에도 있는가"를 항상 확인하는 이 프로젝트의 반복된 습관과 일치한다.
- **#114(미래 회차 오표시)를 증상 수정이 아니라 데이터 상태 수정으로 풀었다.** #109에서 이미 `updateTicketDrawNumber`의 버그(회차 변경 시 matchedRank/status 초기화 누락)를 고쳤는데도 사용자가 같은 증상을 다시 신고한 이유가 "코드가 안 고쳐져서"가 아니라 "#109 이전에 이미 오염된 데이터가 기기에 남아있어서"임을 정확히 구분했다. 재발 방지를 위해 `healFutureTickets`를 매 focus마다 실행하는 자가 치유(self-healing) 로직으로 만들어, 이번 특정 사례뿐 아니라 앞으로 같은 불변조건 위반이 어떤 경로로든 생기더라도 방어되게 한 점이 좋다.
- **#113(기대 대비 실제 1등 당첨자 수)이 기존 통계 인프라를 그대로 확장했다.** 새 계산 로직(`computeFirstPrizeExpectation`)을 `drawStats.ts`에 추가하면서 이미 검증된 `TOTAL_COMBINATIONS`(§probability.ts)를 그대로 재사용했고, 데이터 누락 시(총판매액/1등당첨자수 없음) null을 반환해 화면이 조용히 실패하도록 만들지 않았다. 문구 생성(`describeFirstPrizeExpectation`)이 z-score 구간별로 분기하면서도 "이례적"이라는 표현의 강도를 3단계로 세분화해 과장하지 않은 것도 §23 원칙과 맞다.
- **#117(카드 간격)의 원인이 정확히 진단됐다.** "카드-공지"와 "공지-다음 카드" 사이 여백이 실질적으로 역전(20px vs 8px)되어 있었다는 걸 수치로 짚었고, 해법도 `DisclaimerCard`에 하위호환 `style` prop을 추가하는 최소 변경으로 풀어 이 컴포넌트를 쓰는 다른 6개 화면에 영향이 없다.

### 새로 확인한 리스크 / 남아있는 사항

- **[신규] git 작업 트리에 CRLF/LF 혼재가 있고 이를 막을 `.gitattributes`가 없다.** 이 리포트 작성 중 `git status`로 확인한 결과, `QA_LOG.md`/`data/lotto-draws.json`/`src/lib/deepPattern/combinadic.ts`/`tests/deepPatternCombinadic.test.ts` 4개 파일이 커밋되지 않은 변경으로 잡혀 있는데, 실제 diff는 내용 변경이 아니라 파일 전체가 CRLF로 재저장되며 모든 줄이 교체된 것이었다(`file` 명령으로 확인: 커밋된 버전은 LF, 작업 트리는 CRLF). `git config core.autocrlf`도 설정돼 있지 않다. 지금 당장 기능에 영향은 없지만, 이 상태로 커밋하면 실제 변경 내용이 안 보이는 2만 줄짜리 노이즈 diff가 생기고, 반대로 다른 파일을 편집할 때 에디터가 줄바꿈을 자동 변환하면서 실제 코드 변경이 묻힐 위험이 있다. 저장소 루트에 `.gitattributes`(`* text=auto eol=lf` 등)를 추가하고 한 번 정규화하는 걸 권장한다.
- **[신규] 이 원격 브릿지 환경에서 jest `unit` 프로젝트(순수 로직 테스트)가 45초 내에 끝나지 않았다.** 8/13 리포트까지는 `components`(jest-expo) 프로젝트만 이 세션 환경에서 불안정하다고 기록돼 있었는데, 이번에 `unit` 프로젝트(ts-jest+node, 원래 빠르다고 알려진 쪽)도 두 차례 시도 모두 시간 내에 끝나지 않았다. 사용자 로컬 터미널에서는 여전히 정상 통과할 가능성이 높지만(과거 패턴과 동일), "느린 쪽 프로젝트"의 범위가 이번에 넓어진 점은 기록해둘 필요가 있다 — 원격 브릿지를 통한 디스크 I/O 왕복 비용이 커서 그럴 가능성이 높아 보이나 단정하지는 않는다.
- **[경미] `npx eslint .` 경고 1건**: `app/generate/dice.tsx:130`의 `pressed` 파라미터가 미사용(`no-unused-vars`). 에러는 아니고 기능에 영향 없으나, 다음에 이 파일을 만질 때 `_pressed`로 이름을 바꾸거나 실제로 써서 정리하면 좋다.
- README의 AI 설명 BYOK 관련 서술 불일치(8/13 리포트가 지적)는 **이번에 해소를 확인했다** — 현재 README.md는 "과거에는 BYOK 옵션이 있었으나 사용성 문제로 제거되었다"고 과거형으로 정확히 서술하고 있다.
- New Architecture 전환, `npm audit` 잔여 취약점(아래 2절)은 여전히 Expo SDK 메이저 업그레이드 없이는 근본 해소가 안 되며, 이번 사이클에도 다뤄지지 않아 계속 이월된다.

**점수: 91/100** (8/13과 동일 — #114/#116의 근본 원인 추적 품질은 좋았으나, 두 문제 모두 사용자·CI가 먼저 발견한 뒤에야 대응했다는 점과, 이번에 새로 발견한 git 줄바꿈 혼재·jest `unit` 프로젝트 지연이 상쇄해 순변화는 없다고 판단했다)

---

## 2. 보안책임자 관점

### 재확인한 강점

- **`npm audit --omit=dev` 21건(moderate 11, high 10)** — 8/13(23건) 대비 2건 감소. 새로 실행해 직접 확인했다. 잔여 취약점은 여전히 전부 `@expo/config`/`@expo/config-plugins`/`@expo/prebuild-config` 체인에 몰려 있고, Expo SDK 메이저 업그레이드 없이는 해소되지 않는다는 8/8 리포트 이후의 결론이 이번에도 유효하다.
- **이번 사이클에도 신규 npm 의존성 추가는 0개.** #113~#117 전부 기존 모듈(`drawStats.ts`, `messages.ts`, `DisclaimerCard.tsx`, `tickets.ts`) 내부 확장으로 처리했다.
- 하드코딩된 API 키·시크릿·토큰은 재확인 결과 없음. `data/lotto-draws.json`을 직접 열어 실제 최신 회차(1238회, 2026-08-22 추첨)까지 정상 반영돼 있음을 확인했고, GitHub Actions 자동 동기화 파이프라인이 계속 정상 작동 중임을 데이터로 재확인했다.
- README의 BYOK 관련 문서 불일치(8/13에서 보안/문서 정확성 이슈로 지적)가 해소됨을 위 1절에서 확인했다 — "사용자가 없는 기능이 있다고 오인할 수 있다"는 우려가 정리됐다.

### 새로 확인한 사항

- **[신규, 경미] README.md의 "당첨번호 데이터 파이프라인 (설정 필요)" 절이 현재 상태와 맞지 않는다.** 이 절은 "`data/lotto-draws.json`을 실제 데이터로 채우는 것뿐입니다"라며 초기 설정이 아직 안 된 것처럼 서술하고 있는데, 실제로는 이미 1,238회차(2026-08-22 추첨분까지)가 채워져 있고 GitHub Actions가 매주 자동 갱신 중이다(커밋 이력에서 "chore: update lotto draw data (최신 1238회)" 확인). 8/13 리포트가 지적했던 BYOK 문서 불일치와 같은 유형(문서가 과거 시점에 멈춰 있음)이라, 다음 문서 정리 시점에 함께 손보는 걸 권장한다. 보안 위협은 아니고, 새로 이 저장소를 보는 개발자가 "아직 설정이 안 끝났다"고 오인할 수 있는 정도의 문제다.
- Atlas/당첨이력 자동 동기화의 방어 패턴(항상 null 반환, 구조 검증 후 채택, 실패 시 조용히 폴백)은 8/13과 동일하게 유지되고 있음을 재확인했다. 이번 사이클엔 이 경로에 변경이 없었다.
- dhlottery.co.kr/GitHub raw 요청에 인증서 고정 없음, 설정값 AsyncStorage 평문 저장은 이전과 동일하게 의도적 트레이드오프로 재확인, 재지적하지 않는다.

**점수: 94/100** (8/13 93 대비 +1 — `npm audit` 잔여 건수가 실제로 줄었고 BYOK 문서 불일치가 해소된 점이 근거. 다만 잔여 21건이 네 번째 리포트째 이월되고 있고, 같은 유형의 새 문서 드리프트(당첨번호 파이프라인 절)가 발견돼 큰 폭의 상승은 아니다)

---

## 3. 시니어 기획자 관점 — 사용자 편의성 · 정합성

### 이번에 확인한 개선

- **#113(기대 대비 실제 1등 당첨자 수 카드)은 "그럴듯한 통계"가 아니라 "설명 가능한 통계"를 지향한 판단이다.** 단순히 실제 당첨자 수만 보여주는 게 아니라, 이론적 기대값 대비 지금이 "비슷한 수준"인지 "다소/꽤 이례적"인지를 z-score 기반으로 문장화해서 보여준다 — 판매량이 들쭉날쭉한 로또 특성상 이런 비교 맥락이 없으면 실제 당첨자 수 하나만으로는 사용자가 그 숫자의 의미를 판단하기 어렵다. §23 원칙(과장·오해 방지) 안에서 "전문적으로 보이되 과신을 유도하지 않는" 균형을 잘 잡은 사례로 판단한다.
- **#114(미래 회차 자동 정리)는 신뢰에 직결되는 버그였다.** 아직 추첨도 안 한 회차에 "확인 완료"/"낙첨" 배지가 붙어 있는 건, 통계 카드 문구 하나가 어색한 것과는 차원이 다른 신뢰 손상 요소다 — 사용자가 스크린샷까지 찍어 두 번 신고했다는 사실 자체가 이 문제의 체감 심각도를 보여준다. 자가 치유 방식으로 해결해 같은 유형의 데이터 불일치가 다시 눈에 띄기 전에 조용히 정리되도록 만든 것도 적절하다.
- **#115(무관한 안내 문구 제거)는 작지만 "화면 하단에 이 화면과 상관없는 문구가 붙어 있으면 안 된다"는 원칙을 지킨 사례다.** 인기번호 회피는 AI 조합탐색 화면의 기능인데 로또 연구소 하단에 남아있던 걸 정리했다 — 문구 재사용을 기계적으로 하지 않고 맥락을 따진 판단이다.

### 남아있는 정합성/편의성 격차

- README의 당첨번호 파이프라인 절(위 2절 참고)이 실제 상태보다 뒤처져 있어, 다음에 이 프로젝트를 처음 보는 사람(공동 작업자, 심사자 등)에게 혼선을 줄 수 있다.
- "판매점/1등 배출점 지도 기능"은 2026-08-19 아키텍처 조사(판매점 주소·1등 배출 이력 데이터는 license-clear로 확보 가능했으나, 좌표 변환(geocoding) 경로 7곳을 전부 검토해도 즉시 쓸 수 있는 무료/승인 없는 방법이 없었음) 끝에 사용자 결정으로 전체 보류됐다. 이는 조사가 부실해서가 아니라 실제로 막힌 지점(좌표 변환)이 있었기 때문이므로, "미완성 기능"이 아니라 "근거를 남기고 의도적으로 미룬 로드맵 항목"으로 분류하는 게 정확하다. 나중에 재검토할 때는 SOURCE C(좌표 변환)만 다시 확인하면 된다는 점을 남겨둔다.
- New Architecture 전환 계획은 이번 사이클에도 다뤄지지 않아 계속 이월되고 있다.

**점수: 94/100** (8/13 93 대비 +1 — #114가 신뢰에 직결되는 버그를 근본적으로 해결했고, #113이 통계 기능의 설명 가능성을 한 단계 높인 점이 근거. README 문서 드리프트와 지도 기능 보류 등 남은 격차가 있어 만점과는 거리가 있다)

---

## 4. 시니어 UI/UX 디자이너 관점

### 확인한 강점

- **#117(카드 간격 조정)이 여백을 감(feel)이 아니라 수치로 진단하고 고쳤다.** "관련 있는 내용은 가깝게, 무관한 섹션은 멀게"라는 원칙을 `cardTight`(6px)/`attachedNotice`(12px)라는 두 스타일로 명확히 구분해 로또 연구소의 3개 카드-공지 쌍에 일관되게 적용했다. `DisclaimerCard`에 하위호환 `style` prop을 추가하는 방식도, 다른 6개 화면(exclusion/dice/qr-check/SettingsSheet/lucky/ai-search/destiny)에 쓰이는 기본 여백을 전혀 건드리지 않아 리스크가 낮다.
- **#113의 카드 배치(조합 패턴 통계와 내 번호 분석 사이)가 로또 연구소의 기존 정보 위계를 그대로 따른다.** 새 카드를 화면 맨 아래에 욱여넣지 않고, 성격이 비슷한 통계 카드들 사이에 자연스럽게 끼워 넣어 화면을 처음부터 다시 훑어봐야 할 필요가 없게 했다.
- 결과 팝업 문구 개선(#85, 8/13 리포트)과 이번 #114의 자동 정리가 합쳐져, "내 번호" 탭이 실제 추첨 시점과 어긋나는 상태를 보여주는 경우가 이제 구조적으로 없어졌다 — UX 디자이너 관점에서 "화면이 거짓말하지 않는다"는 원칙은 문구 톤보다 우선한다.

### 남아있는 사항

- 8/13 리포트가 지적한, 아직 실기기에서 눈으로 확인되지 않은 딥 패턴 화면군 변경 다수가 이번 사이클에도 별도로 재확인되지 않았다 — 이번 사이클의 초점이 lab.tsx/tickets.tsx/CI 쪽이었기 때문에 범위 밖이었을 뿐, 해소된 것은 아니다.
- 다크모드 수동 토글 부재는 계속 낮은 우선순위로 남아있다.

**점수: 94/100** (8/13 93 대비 +1 — #117이 여백 문제를 수치 기반으로 정확히 진단·해결했고, #114가 "화면이 실제 상태와 어긋나 보이는" 근본적인 UX 신뢰 문제를 없앤 점이 근거. 8/13에서 이월된 실기기 미확인 항목들은 이번 사이클 범위 밖이라 그대로 남아 만점과는 거리가 있다)

---

## 5. 경쟁 앱 대비 포지션

8/13 리포트와 마찬가지로 이번 사이클도 별도의 경쟁 앱 재조사는 하지 않았다(다만 6절의 GRAC 조사 과정에서 실제 경쟁 앱들의 카테고리 등록 방식을 확인한 것은 있다). 아래는 8/13 조사 결과에 이번 사이클의 변화만 반영한 것이다.

### 경쟁력 있는 부분

- **"기대 대비 실제 1등 당첨자 수"(#113)는 국내 인기 로또 앱들이 잘 다루지 않는 종류의 설명이다.** 실제 당첨자 수를 그냥 보여주는 앱은 많지만, 그 숫자가 "통계적으로 이례적인지"를 이론적 기대값과 비교해 문장으로 설명해주는 기능은 8/13 리포트가 지적한 "통계는 탄탄한데 흔한 경쟁 앱은 이 정도까지 안 간다"는 강점을 한 겹 더 강화한다.
- 8/13 리포트에서 확인된 QR 당첨확인, 딥 패턴 탐색의 상시 최신화, 온디바이스 알림 등의 경쟁력은 이번 사이클에서 변경되지 않아 그대로 유지된다.

### 남은 격차

- 딥 패턴 탐색의 실기기 체감 속도는 8/8 리포트가 지적한 이후 여전히 실측 확인이 이월되고 있다.
- 판매점 찾기 기능은 8/13까지 "낮은 우선순위 격차"로 남아있었는데, 이번에 아키텍처 조사까지 마친 뒤 명시적으로 보류됐다(위 3절 참고) — 격차가 사라진 건 아니지만, 최소한 "왜 아직 없는지"에 대한 근거는 명확해졌다.

**점수: 89/100** (8/13 88 대비 +1 — 새 통계 설명 기능이 기존 차별점을 강화한 것과, GRAC/Play Store 조사로 실제 출시 실행 리스크가 낮아진 것이 근거. 경쟁 앱 재조사 자체는 이번에도 하지 않았고 체감 속도 미검증이 이월되고 있어 큰 폭의 상승은 아니다)

---

## 6. 출시 준비 상태 — 구글 플레이스토어 · GRAC

이번 사이클에 병행한, 코드 변경은 아니지만 "출시가 실제로 가능한 상태인가"에 답하는 조사다. 전체 내용은 별도 파일(`구글플레이스토어_출시_절차.md`)로 전달했고, 여기서는 이 리포트의 다른 절과 같은 기준(무엇을 직접 확인했고 무엇이 근거 부족인지)으로 요약한다.

- **타겟 API 레벨(2026-08-31 마감, API 36)**: `app.json`에 별도 재정의가 없어 Expo SDK 54 기본값(API 36)을 그대로 따른다 — 이 프로젝트는 **이미 요건을 충족한 상태**임을 코드로 직접 확인했다.
- **빌드 형식**: `eas.json`의 `production` 프로필에 `android.buildType` 재정의가 없어 EAS 기본값인 `.aab`로 빌드된다 — Play가 요구하는 형식과 일치한다. `preview` 프로필만 `.apk`로 별도 지정돼 있어(테스트 빌드 용도), 실수로 `preview`를 스토어에 제출하지 않도록 주의가 필요하다는 점을 짚어뒀다.
- **GRAC 등급분류**: grac.or.kr 자체는 자동 조회가 막혀 있어 1차 자료로 직접 검증할 수 없었다(이 세션의 웹 콘텐츠 정책상 우회 시도는 하지 않았다). 대신 (1) 게임산업진흥에관한법률상 "사행성게임물" 정의(베팅·배당 또는 재산상 이익 발생 필요 — 이 앱은 해당 없음), (2) Google Play 공식 한국 정책 페이지(GRAC 심의는 "게임" 카테고리 앱에만 적용), (3) 실제 서비스 중인 동종 앱 5개 전부가 "도구/생산성/엔터테인먼트" 카테고리로 등록돼 GRAC 배지 없이 운영 중인 사례, 이 세 갈래를 근거로 "게임이 아닌 카테고리로 등록하면 GRAC 절차 대상이 아닐 가능성이 높다"고 결론지었다. **다만 이는 2차 자료 기반 판단이라 100% 확정은 아니며, 완전한 확신이 필요하면 GRAC(1566-9060) 직접 문의를 권장한다**는 점을 사용자에게도 전달했다.
- **신규 개인 개발자 계정 요건**: 2023-11-13 이후 개설 계정은 정식 출시 전 비공개 테스트(테스터 12명, 14일 연속 옵트인)가 필수다 — 이 저장소의 실제 EAS/앱 설정과는 무관한 계정 정책이지만, 일정 계획(최소 2주 확보)에 직접 영향을 준다.

이 절은 4개 관점 점수에는 반영하지 않았다 — 코드 품질이나 UX가 아니라 "출시 프로세스 리스크"를 다루는 별개의 축이기 때문이다. 다만 GRAC 관련 최종 확인(1566-9060 문의 또는 브라우저 직접 접속)은 스토어 등록정보 작성 전에 처리하는 걸 권장한다.

---

## 우선순위별 개선 제안

**빠른 저비용 개선 (Low effort)**
1. 저장소 루트에 `.gitattributes`(`* text=auto eol=lf` 등)를 추가하고 현재 CRLF로 바뀐 4개 파일을 LF로 정규화한다 — 지금 커밋하면 실제 변경 없이 2만 줄 규모의 노이즈 diff가 생기는 상태다.
2. `app/generate/dice.tsx:130`의 미사용 `pressed` 파라미터 정리(`_pressed`로 이름 변경 또는 실제 사용) — eslint 경고 1건.
3. README.md의 "당첨번호 데이터 파이프라인 (설정 필요)" 절을 현재 상태(이미 1,238회차까지 자동 동기화 중)에 맞게 갱신 — BYOK 절과 같은 유형의 문서 드리프트다.
4. GRAC 관련 최종 확인(1566-9060 문의 또는 grac.or.kr 브라우저 직접 접속)을 스토어 등록 전에 마무리한다.

**중기 개선**
5. Play Console 개발자 계정 등록(신원 인증 대기 시간 고려해 가장 먼저 시작), 비공개 테스트 12명/14일 요건에 맞춘 일정 계획.
6. jest `unit` 프로젝트가 이 원격 브릿지 환경에서 왜 45초 내에 끝나지 않는지 — 급하지는 않지만, 사용자 로컬 실행 결과와 이 세션 환경 결과가 계속 갈리는 원인을 한 번은 파악해두면 다음 세션에서 매번 새로 놀라지 않는다.

**장기 계획**
7. Expo SDK 54→57 업그레이드(New Architecture 전환 포함) — 세 번째 리포트에 이어 이번에도 이월. `npm audit` 잔여 21건 전체의 유일한 해소 경로다.
8. Android edge-to-edge(API 36) 실기기 육안 확인 — 계속 미확인.
9. "판매점/1등 배출점 지도 기능"은 SOURCE C(좌표 변환) 경로가 새로 열리면(예: 행안부 위치정보 API 승인, 또는 무료 tier 정책 변경) 재검토 대상으로 남겨둔다.

---

## 검증 방법 및 한계

이 리포트는 코드 정적 분석(원격 기기 브릿지로 `src/`, `app/`, 루트 문서 파일들을 직접 열람), QA_LOG.md #113~#117 전체 원문 재검토, `APP_REVIEW_2026-08-13.md`와의 대조, 그리고 이번 리포트 작성 과정에서 이 세션이 직접 재실행한 `npx tsc --noEmit`(클린), `npx eslint .`(경고 1건), `npm audit --omit=dev`(21건), `git status`/`git diff`(CRLF 혼재 발견), `node -e`로 `data/lotto-draws.json` 직접 조회(1,238회차까지 정상 반영 확인)를 근거로 삼았다. jest `unit`/`components` 두 프로젝트 모두 이 세션의 45초 제한 내에 완료되지 않아 전체 테스트 스위트의 pass/fail 개수는 이번에 직접 재확인하지 못했다 — 사용자 로컬 터미널에서의 직접 실행 결과를 참고하는 걸 권장한다.

**가장 중요한 한계**: 8/13 리포트가 지적한 "딥 패턴 화면군 등 대량 UI 변경의 실기기 미검증"은 이번 사이클의 작업 범위(lab.tsx/tickets.tsx/CI/문서) 밖이라 이번에도 해소되지 않았다 — 별도로 실기기 훑어보기가 필요하다. GRAC 결론은 1차 자료(grac.or.kr) 직접 확인 없이 2차 자료·판례로 도출한 것이라는 한계를 다시 한번 밝힌다. 경쟁 앱 재조사(GRAC 조사 과정의 카테고리 확인 제외)는 이번 사이클에서도 다루지 않았다.
