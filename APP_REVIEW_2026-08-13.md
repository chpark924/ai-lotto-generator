# 금손로또 종합 재점검 리포트 (2026-08-13)

시니어 개발자 · 보안책임자 · 시니어 기획자 · 시니어 UI/UX 디자이너 관점에서, `APP_REVIEW_2026-08-08.md`(직전 리포트) 이후 5일간 쌓인 QA_LOG #58~#87(30개 항목)을 코드 레벨로 재검증했다. 이번 사이클은 세 갈래로 나뉜다. (1) 실기기 테스트 이전에 진행한 사전 작업 — Combinadic rank/unrank(#59), `npm audit` 실제 조사 및 안전한 해소(#60). (2) 실기기 QA 1차전 — 홈/번호 만들기/45면체 주사위/딥 패턴 탐색 화면 전반에 걸친 UX 폴리시 대량 반영(#61~#76), 그리고 딥 패턴 Atlas 최신화 방식을 "앱 재빌드 필요"에서 "GitHub 자동 동기화, 재빌드 불필요"로 바꾼 이번 사이클 최대 아키텍처 변경(#78, #79). (3) 실기기 QA 2차전(이번 세션, #80~#87) — 당첨결과 카드 구분, 장기 미출현 번호 정확도, 그래프 스크롤 힌트, 티켓 카드 가독성, 결과 팝업 문구, 그리고 홈→번호 만들기 내비게이션 패턴을 모달로 바꿨다가 사용자가 직접 재검토를 요청해 표준 push로 되돌린 자기 교정 사이클(#86→#87)이 포함된다.

**이 리포트를 읽기 전에 반드시 알아야 할 것**: 이 리포트 작성 시점 기준 사용자가 `eas build --platform android --profile preview`를 진행 중이다(로그: `.../builds/6a470fe2-...`, 아직 완료 안 됨). 즉 #61~#87에서 다룬 UI 변경 전부(홈 화면 여백, 딥 패턴 슬라이더/애니메이션/Pattern Map, 45면체 프레스 피드백, 당첨결과 배지, 내비게이션 패턴 되돌리기 등)가 **이 리포트 작성 시점까지 단 한 번도 실기기에서 눈으로 확인된 적이 없다**. `npx tsc --noEmit`/`npx eslint .`/`npx jest`는 항목별로 꾸준히 클린했지만, 이 세 가지는 레이아웃이 실제로 안 깨졌는지, 애니메이션 타이밍이 자연스러운지, 터치 반응이 매끄러운지를 검증하지 못한다 — 이번 EAS 빌드가 완료되고 실기기에서 훑어보는 것이 이 사이클 전체의 최종 검증 단계다.

## 종합 점수 비교

| 영역 | 8/8 점수 | 8/13 재점검 | 변화 |
|---|---|---|---|
| 보안 | 93 | **93** | – |
| 안정성 · 코드 품질 | 90 | **91** | ▲ 1 |
| 최적화 | 91 | **91** | – |
| 사용자 편의성(UX/UI) | 92 | **93** | ▲ 1 |
| 경쟁 앱 대비 강점 | 87 | **88** | ▲ 1 |
| **종합** | **~91** | **~91 / 100** | – |

종합 점수가 사실상 제자리인 이유를 먼저 밝힌다. 이번 사이클은 개별 판단의 질(딥 패턴 Atlas 아키텍처 전환, 내비게이션 패턴 자기 교정)은 8/8 이후 오히려 더 성숙했다고 본다. 하지만 (1) 그 판단들의 최종 결과물이 아직 실기기에서 전혀 검증되지 않았고, (2) `npm audit` 잔여 23건이 여전히 Expo SDK 메이저 업그레이드 없이는 못 없앤다는 사실이 세 번째 리포트(8/6→8/8→8/13)에 걸쳐 그대로 이월되고 있어서, 점수를 올릴 근거보다 "다음 확인이 남아있다"는 유보가 더 크다고 판단했다.

---

## 1. 시니어 개발자 관점 — 안정성 · 코드 품질

### 확인한 강점

- **딥 패턴 Atlas 아키텍처 전환(#78, #79)이 이번 사이클 최대 성과다.** 기존에는 `data/deep-pattern-atlas.json`이 앱에 정적 번들되어 있어, 매주 자동 갱신되는 당첨 이력과 달리 딥 패턴 결과는 앱을 재빌드·재배포해야만 최신화됐다(#77에서 실측: 앱 자체 회차 추정식은 1236회인데 atlas는 8/8 빌드 시점 1235회에 멈춰 있었음). 이를 이미 검증된 당첨 이력 동기화 패턴(`githubDataSource.ts`+`drawCache.ts`)과 동일한 구조로 `atlasGithubSource.ts`+`atlasCache.ts`를 새로 만들어 그대로 적용했다 — 새 아이디어를 발명하지 않고 이미 신뢰성이 검증된 자체 패턴을 재사용한 점이 합리적이다. `isPlausibleAtlas()`로 응답 구조를 검증하고, 실패 시 항상 조용히 번들된 atlas로 폴백하며, 받아온 atlas가 기존보다 회차가 더 높을 때만 채택하는 방어적 설계도 기존 원칙 그대로다.
- **테스트 환경과 실제 런타임의 차이를 미리 진단하고 격리했다(#78).** `atlasCache.ts`가 쓰는 `AsyncStorage`가 unit 테스트 프로젝트(ts-jest+node, RN 목 없음)에서 실제로 부르면 깨진다는 걸 `node -e`로 미리 재현 확인한 뒤, 네트워크가 필요한 `refreshAtlasIfStale()`을 `engine.ts` 내부 자동 호출이 아니라 화면(`deep-pattern.tsx`)에서만 명시적으로 부르도록 분리해 기존 테스트가 항상 결정론적으로 돌게 만들었다 — "손 안 댄 경로는 기존과 완전히 동일하게 동작한다"는 이 프로젝트의 반복된 설계 원칙이 이번에도 지켜졌다.
- **실기기에서 실제로 터진 버그를 근본 원인까지 추적해 고쳤다(#79).** #78 검증을 위해 사용자가 실기기 터미널에서 재실행하자 "Cannot use import statement outside a module" 에러가 났다 — `atlasCache.ts` → `storage.ts` → `secureStorage.ts` → `expo-secure-store`(ESM 전용) 체인을 정확히 짚어내고, `storage.ts`를 아예 안 거치도록 `AsyncStorage`를 직접 쓰는 축소판으로 재구현해 해결했다. "왜 이번에 처음 이 문제가 드러났는가"(engine.ts가 처음으로 storage.ts를 간접 참조하게 됨)까지 설명이 되는 원인 규명이라 재발 가능성도 낮다.
- **자기 발견 CI 회귀도 스스로 잡았다(#80).** #72~#79를 푸시한 뒤 `ci.yml`이 실패했는데, 원인 2개(엔진 mock에 신규 함수 누락, #71에서 이미 제거하기로 한 문구를 여전히 검사하던 stale assertion) 모두 이번 사이클 코드가 아니라 과거 리팩터가 남긴 부채였음을 정확히 구분해 설명하고 고쳤다.
- **Combinadic 사전 작업(#59)의 검증 방식이 견고하다.** golden value(사전순 첫/마지막, 자릿수 캐리 경계값 수기 계산), 처음 12,000개 조합 전수 일치 확인, 결정론적 PRNG 기반 round-trip 2,000회, 실제 Atlas 데이터 교차검증까지 §34(Exhaustive/Golden/Property Test) 원칙을 충실히 지켰다. 아직 어디서도 호출되지 않는 "준비 코드"라는 점도 코드 docblock에 정직하게 남겨뒀다.
- `npm audit` 조사(#60)에서 "고칠 수 있는 것과 없는 것"을 `fixAvailable` 필드로 실제로 구분해, 안전한 패치(`brace-expansion`)만 `package.json` 변경 없이 lockfile 갱신으로 반영했다(25건→23건). 나머지 23건이 전부 Expo SDK 메이저 업그레이드가 필요하다는 사실을 근거와 함께 명시하고 손대지 않은 판단도 적절하다.

### 새로 확인한 리스크 / 남아있는 사항

- **#61~#76, #81~#87의 UI 변경 대부분에 대응하는 신규 자동 테스트가 없다.** 레이아웃 수치, 애니메이션 타이밍, 프레스 피드백, 스크롤 페이드 같은 시각적 변경은 원래 자동 테스트로 잡기 어려운 영역이라 `tsc`/`eslint` 클린 + 실기기 육안 확인에 의존하는 이 프로젝트의 기존 방식 자체는 합리적이다. 다만 그 육안 확인이 이 리포트 시점까지 하나도 이뤄지지 않았다는 점(위 경고 참고)이 지금 가장 큰 미확인 리스크다.
- `components`(jest-expo) 프로젝트가 이 원격 세션 환경에서 반복적으로 불안정했다(#58, #72, #78에서 각각 다른 형태로 — 5000ms 타임아웃, 40초 안팎의 느린 디스크 I/O, 45초 내 미완료)는 패턴이 이번 사이클에도 계속됐다. 사용자 로컬 터미널에서는 매번 정상 통과가 확인돼 "코드 결함이 아니라 세션 환경 특유의 문제"라는 8/8 리포트의 결론이 이번에도 유효하지만, 완전히 해소되지는 않은 채로 세 번째 사이클째 이월되고 있다.
- New Architecture 전환 계획(8/6 리포트 최초 지적)은 이번 사이클에도 다뤄지지 않았다 — 아래 npm audit 항목과 사실상 같은 근본 원인(Expo SDK 54→57 업그레이드)이라 함께 계획하는 게 맞다.

**점수: 91/100** (8/8 90 대비 +1 — Atlas 아키텍처 전환에서 테스트 격리·실기기 버그 추적·CI 자기 회귀 수정까지 엔지니어링 기본기가 일관되게 지켜진 점이 근거. 대량의 UI 변경이 아직 실기기 미검증이고 `components` 테스트 환경 불안정성이 계속 이월되고 있어 큰 폭의 상승은 아니다)

---

## 2. 보안책임자 관점

### 재확인한 강점

- **Atlas 자동 동기화(#78)가 새 네트워크 표면을 열었지만, 기존과 동일한 방어 원칙을 그대로 적용했다.** `atlasGithubSource.ts`는 절대 throw하지 않고 실패 시 항상 null을 반환하며, `isPlausibleAtlas()`로 받아온 데이터가 구조적으로 그럴듯한지 검증한 뒤에만 채택한다 — "받아왔다고 그대로 믿지 않는다"는 당첨 이력 쪽 원칙(`isPlausibleWinningDraw`)과 정확히 같은 패턴이라 새 코드가 새로운 취약점 클래스를 만들지 않았다.
- `npm audit --omit=dev` **23건(moderate 11, high 12)** — 8/8(25건) 대비 감소. `brace-expansion` 관련 high 2건이 `package.json` 변경 없이 lockfile 갱신만으로 해소됐다(#60).
- 이번 사이클에서 추가된 신규 npm 의존성은 0개다(#72의 커스텀 슬라이더도 별도 라이브러리 대신 RN 코어 `PanResponder`로 직접 구현 — "새 네이티브 모듈 추가 시 EAS 재빌드가 필요해진다"는 이유를 명시하고 의도적으로 피한 판단). 공급망 위험이 늘지 않았다.
- 하드코딩된 API 키·시크릿·토큰은 재확인 결과 없음.

### 새로 확인한 사항

- **README.md와 실제 코드의 불일치가 아직 해소되지 않았다.** `SECURITY_NOTES.md`(2026-07-31)가 이미 "README는 AI 설명 BYOK(사용자 API 키 입력) 옵션이 있다고 서술하지만, 실제 코드(`src/lib/ai/explain.ts`)는 이 기능을 제거하고 로컬 템플릿 설명만 제공한다"고 지적해뒀는데, 이번에 `explain.ts`(docblock에 "이전에는 BYOK 옵션이 있었으나 사용성 관점에서 제거했다"고 명시)와 `SettingsSheet.tsx`(AI/API 관련 코드 전무)를 직접 열어 재확인한 결과 코드 쪽은 이미 완전히 정리됐지만, `README.md`는 여전히 "설정에서 'AI 설명 사용'을 켜고 본인의 Anthropic API 키를 입력한 경우에만 `explainGameWithAi`가 호출됩니다"라고 존재하지 않는 기능을 설명하고 있다. 보안 위협은 아니지만, 앱을 처음 접하는 개발자나 검토자가 README만 보고 실제로 없는 기능(외부 API 키 입력·전송)이 있다고 오인할 수 있어 "개인정보 처리 관점의 문서 정확성" 이슈로 분류해뒀다 — 2주 가까이 이월된 낮은 우선순위 항목이라 이번엔 우선순위 목록에 정식으로 올린다.
- dhlottery.co.kr/GitHub raw 요청에 인증서 고정 없음, 설정값 AsyncStorage 평문 저장은 8/8과 동일하게 의도적 트레이드오프로 재확인, 재지적하지 않는다.

**점수: 93/100** (8/8과 동일 — Atlas 신규 네트워크 표면을 기존 검증된 방어 패턴 그대로 적용해 위험을 늘리지 않은 점은 긍정적이나, `npm audit` 잔여 23건이 세 번째 리포트째 이월되고 README 문서 불일치도 2주 가까이 미해결이라 상승 요인이 하락 요인을 상쇄한다)

---

## 3. 시니어 기획자 관점 — 사용자 편의성 · 정합성

### 이번에 확인한 개선

- **딥 패턴 탐색이 "서버 비용 0원" 원칙을 지키면서도 상시 최신화된다는 건 실제 제품 차별점이다(#78).** 통계 기반 기능이 배포 주기에 묶이지 않고 매주 자동으로 최신 회차를 반영하게 된 것은, 앱을 자주 업데이트하지 않는 사용자에게도 "신선한" 결과를 보장한다는 뜻이라 기획 관점에서 가치가 크다.
- **내비게이션 패턴을 사용자 실사용 피드백으로 재검토하고 되돌린 사이클(#86→#87)이 이번 사이클에서 가장 성숙한 의사결정 과정이었다.** 처음엔(#86) "홈에서 들어가면 돌아갈 방법이 안 보인다"는 문제를 모달(아래→위)로 풀었는데, 사용자가 실기기에서 써보고 "이게 정말 일반적인 패턴이냐"고 재차 확인을 요청하자 방어하지 않고 원점에서 재검토해 "모달은 작성/설정 같은 짧은 일회성 작업에 쓰는 패턴이지, '번호 만들기' 탭의 핵심 기능 자체에는 안 맞는다"는 결론으로 표준 push + 기본 뒤로가기 버튼으로 되돌렸다. 원래 문제(돌아갈 방법이 안 보임)는 중첩 스택 구조(`app/generate/_layout.tsx`)의 표준 pop 동작으로 여전히 해결된 상태를 유지하면서, 잘못 골랐던 프레젠테이션 방식만 걷어낸 점이 깔끔하다.
- 데이터 정합성 개선도 두 건 있었다: "제N회 당첨결과" 카드를 통계 카드와 시각적으로 구분해 "이건 실제 결과다"를 명확히 했고(#81), "장기 미출현 번호"에 보너스 번호 출현도 반영해 실제로는 최근에 나온 번호가 계속 "미출현"으로 잘못 표시되는 문제를 없앴다(#82) — 둘 다 "그럴듯해 보이지만 실제로는 틀린 정보"를 정직하게 바로잡은 사례로, 이 프로젝트가 반복해서 지켜온 "과장·오해 방지" 원칙과 일치한다.
- 결과 팝업 문구를 기계적 "결과: 낙첨"에서 "안타깝게도 낙첨되었습니다"/"OO등에 당첨되셨어요!"로 바꾼 것(#85)은 작은 변경이지만, 로또 앱처럼 감정이 개입되는 순간의 카피는 체감 품질에 미치는 영향이 크다.

### 남아있는 정합성/편의성 격차

- 이번 사이클 UX 변경 전부가 실기기 미확인 상태다 — 특히 #66~#67(딥 패턴 카드 발견성), #71~#75(딥 패턴 인트로/로딩/상세 화면 대개편)처럼 시각적 임팩트가 큰 변경들은 다음 실기기 세션에서 최우선으로 훑어봐야 한다.
- README.md의 AI BYOK 관련 서술 정정(위 보안 섹션 참고) — 사용자 대면 문서는 아니지만 다음 배포 준비 문서 정리 시점에 함께 처리하면 좋다.
- New Architecture 전환 계획은 이번 사이클에도 다뤄지지 않아 계속 이월되고 있다.

**점수: 93/100** (8/8 92 대비 +1 — Atlas 상시 최신화라는 실질적 차별점과, 잘못된 UX 판단을 사용자 피드백으로 재검토해 되돌린 성숙한 의사결정 과정이 근거. 실기기 미확인 상태인 변경이 많이 쌓여 있어 큰 폭의 상승은 아니다)

---

## 4. 시니어 UI/UX 디자이너 관점

### 확인한 강점

- **내비게이션 패턴 재검토(#86→#87)는 UX 원칙에 부합한다.** 모달(bottom sheet 스타일 슬라이드)은 토스·카카오뱅크류 앱에서도 보통 작성·필터·설정처럼 짧고 되돌아올 필요가 없는 일회성 작업에 쓰이고, "번호 만들기" 탭처럼 여러 단계를 오래 머무는 핵심 기능 진입에는 오른쪽에서 들어오는 표준 push + 네이티브 '‹' 뒤로가기가 사용자가 이미 학습한 가장 예측 가능한 패턴이다. 커스텀 헤더 버튼을 만들지 않고 React Navigation의 표준 동작(중첩 스택 pop)에 맡긴 것도 유지보수 관점에서 바람직하다.
- **딥 패턴 탐색 화면군의 UX 디테일이 이번 사이클에서 가장 밀도 있게 다듬어졌다.** 인트로 화면은 긴 설명 대신 시각 효과(점-선 애니메이션)를 먼저 보여주도록 재배치되고 중복 경고 문구가 제거됐으며(#71), 혼합 슬라이더는 처음엔 넓고 두꺼웠다가 사용자 피드백으로 얇고 좁게 조정됐고(#72→#73), 로딩 애니메이션이 다 그려지기 전에 화면이 전환되던 타이밍 버그가 원인(900ms vs 실제 필요 1440ms)까지 정확히 짚여 고쳐졌으며(#74), "Pattern Map"이 고정된 예시 그림에서 실제 지표 기반 근사 좌표로 교체됐다(#75) — 각 항목이 "예시/더미"를 "실제 데이터"로 바꾸거나 "장식"을 "의미 있는 피드백"으로 바꾸는 일관된 방향을 갖고 있다.
- **작지만 신뢰를 갉아먹을 수 있었던 오해 소지 UI가 두 건 정리됐다**: 45면체 주사위 구슬에 눌러도 반응 없는데 "탭해서 굴려보세요"라고 써 있던 문구 제거(#69), 버튼 3개를 눌러도 스타일 변화가 전혀 없어 이미 선택된 것처럼 보이던 문제를 앱의 기존 프레스 피드백 관례(어둡게+축소+리플)로 통일(#70) — 둘 다 "기능은 동작하지만 사용자에게 잘못된 신호를 주는" 유형이라, 실기기에서 실제로 눌러보지 않으면 코드 리뷰만으로는 잘 안 잡히는 부류의 결함이다.
- **필수 CTA가 시스템 내비게이션 바에 가려지던 안전 이슈를 재발 방지까지 확인하며 고쳤다(#76).** 원인이 이 화면만 공용 `BottomActionBar`(안전영역 자동 반영) 관례에서 벗어나 있었던 것임을 정확히 짚고, 같은 문제가 다른 화면에도 있는지 전수 점검까지 마쳤다.

### 남아있는 사항

- 위에서 반복 언급한 대로, 이번 사이클의 시각·인터랙션 변경(특히 딥 패턴 화면군 5건, 45면체 2건, 홈 화면 여백 3건)이 실기기에서 전혀 확인되지 않았다 — UX 디자이너 관점에서는 이게 가장 큰 미해결 리스크다. 특히 #73의 슬라이더 두께/터치 판정, #75의 Pattern Map 좌표 배치처럼 "수치로만 조정했지 실제 화면에서 어떻게 보이는지"가 중요한 항목들.
- 다크모드 수동 토글 부재는 8/8과 동일하게 낮은 우선순위로 남아있다.

**점수: 93/100** (8/8 92 대비 +1 — 잘못된 UX 판단을 사용자 재검토 요청에 방어적이지 않게 응해 원칙에 맞게 되돌린 것과, 딥 패턴 화면군의 "더미를 실제 데이터로" 일관된 개선 방향이 근거. 실기기 미확인 항목이 많이 쌓여 있어 만점과는 거리가 있다)

---

## 5. 경쟁 앱 대비 포지션

8/8 리포트 이후 별도의 경쟁 앱 재조사는 진행하지 않았다(8/8과 동일하게 이번 사이클도 신규 기능 완성도·UX 폴리시에 집중됐다) — 아래는 8/8 조사 결과에 이번 사이클의 변화만 반영한 것이다.

### 경쟁력 있는 부분

- **"딥 패턴 탐색"이 통계적 엄밀성(8/8까지 확보)에 이어 "상시 최신화"라는 운영 차별점까지 갖췄다(#78).** 국내 인기 로또 앱들이 이 정도의 통계 엄밀성(kNN 보정, Null 시뮬레이션, 다중검정 보정)을 갖춘 경우 자체가 8/8 조사에서도 드물었는데, 그 결과를 서버 비용 없이 매주 자동으로 최신화한다는 점까지 더해져 "통계는 탄탄한데 데이터가 며칠 지난 정보"라는 흔한 약점을 없앴다.
- 8/8 리포트에서 확인된 QR 당첨확인, 통계 깊이, 온디바이스 알림 등의 경쟁력은 이번 사이클에서 변경되지 않아 그대로 유지된다.

### 남은 격차

- 딥 패턴 탐색의 실기기 체감 속도는 8/8 리포트가 지적한 이후에도 여전히 검증되지 않았다 — 이번 세션의 EAS 빌드가 이 확인을 위한 것이므로, 완료 즉시 최우선으로 실측해야 한다.
- 판매점 찾기, 구매 리마인드와 딥 패턴 결과 연동 등은 8/8과 동일하게 낮은 우선순위로 남아있다.

**점수: 88/100** (8/8 87 대비 +1 — 딥 패턴 탐색이 통계 엄밀성에 "상시 최신화"까지 더해지며 차별점이 한층 견고해진 것이 근거. 이번 사이클에도 경쟁 앱 재조사는 하지 않았고, 차별점의 핵심인 체감 속도가 여전히 미검증이라 큰 폭의 상승은 아니다)

---

## 우선순위별 개선 제안

**즉시 (실기기 빌드 완료 즉시)**
1. 진행 중인 EAS 빌드(preview) 설치 후, #61~#87 전체를 실기기에서 한 번씩 훑어본다 — 특히 딥 패턴 화면군(인트로/로딩/슬라이더/상세 Pattern Map), 45면체 프레스 피드백, 홈→번호 만들기 내비게이션(#87의 표준 push 되돌리기가 실제로 자연스러운지), 딥 패턴 탐색의 체감 속도(§55~56에서 구조적으로 손댔지만 실기기 실측은 8/8부터 계속 이월).

**빠른 저비용 개선 (Low effort)**
2. `README.md`의 AI 설명 BYOK 관련 서술을 실제 코드(로컬 템플릿만 제공, API 키 입력 기능 없음)에 맞게 정정 — `SECURITY_NOTES.md`가 2주 가까이 전에 이미 지적해둔 항목.
3. `components`(jest-expo) 프로젝트가 이 원격 세션 환경에서 반복적으로 불안정한 원인을 한 번은 근본적으로 파봐도 좋다(다만 사용자 로컬에서는 매번 정상이라 급하지 않음) — 혹은 이 세션 환경에서는 애초에 개별 파일 단위로만 돌리는 것을 기본 워크플로로 문서화해 매번 새로 놀라지 않게 한다.

**중기 개선**
4. 딥 패턴 Combinadic 유틸(#59)을 실제로 쓸 곳(basin 표본 정교화, §7 Exact 해상도 계층)이 생기면 연결.
5. 딥 패턴 탐색 실기기 latency 실측 후, 필요하면 `kNearestVoidScore`의 top-k 선택 방식이나 `CANDIDATE_POOL_SIZE`/`sampleCombos` 크기 조정(QA_LOG #56에 후보로 기록됨).

**장기 계획**
6. Expo SDK 54→57 업그레이드(New Architecture 전환 포함) — 8/6 리포트부터 세 번째 리포트째 이월 중이며, `npm audit` 잔여 23건 전체의 유일한 해소 경로이기도 하다. 다음 여유 있는 사이클에 정식으로 계획을 잡을 것을 권장한다.
7. Android edge-to-edge(API 36) 실기기 육안 확인 — 8/6부터 계속 미확인.

---

## 검증 방법 및 한계

이 리포트는 코드 정적 분석(원격 기기 브릿지로 `src/`, `app/`, `data/`, 루트 문서 파일들을 직접 열람), QA_LOG.md #58~#87 전체 원문 재검토, `APP_REVIEW_2026-08-08.md`와의 대조, 그리고 각 QA_LOG 항목에 기록된 `npx tsc --noEmit`/`npx eslint .`/`npx jest`(사용자가 실기기 터미널에서 직접 실행한 결과) 기록을 근거로 작성했다. 이번 리포트 작성 과정에서 이 세션이 직접 `tsc`/`eslint`/`jest`를 재실행하지는 않았다 — 이미 각 항목이 개별적으로 검증·커밋된 상태이고, 이 리포트의 목적은 그 결과들을 종합해 재해석하는 데 있기 때문이다. 대신 `README.md` vs `src/lib/ai/explain.ts` vs `src/components/SettingsSheet.tsx`의 실제 코드 대조처럼, 이전 리포트들이 남긴 미해결 항목을 이번에 직접 파일을 열어 재확인하는 작업은 이번 리포트에서 새로 수행했다.

**가장 중요한 한계**: 이 리포트가 다루는 30개 QA_LOG 항목 중 절대다수(#61~#87)가 실기기 화면에서 시각적으로 확인된 적이 없다 — 사용자가 EAS 빌드 쿼터 문제로 실기기 테스트를 의도적으로 한 번에 몰아서 진행하기로 했고, 그 빌드가 이 리포트 작성 시점 기준 아직 진행 중이기 때문이다. 따라서 이 리포트의 점수, 특히 "사용자 편의성(UX/UI)" 항목은 "코드가 의도한 대로 동작한다면"이라는 전제 위에 있다 — 실기기 확인 후 재조정이 필요할 가능성을 열어둔다. 경쟁 앱 재조사, 스토어 등록 전 최종 QA는 이번 사이클에서도 다루지 않았다.
